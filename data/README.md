# data/ — Data & ingestion layer (ARCHITECTURE.md §3-4)

Barchart CSV ingestion (parser), futures-curve construction, and real-data replay. Future home of the partitioned HDB load/query. Raw CSVs under `data/barchart/` (gitignored) are an ingestion source, not the store.
