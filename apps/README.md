# apps/ — Applications layer

Examples, report generators, and CLIs that exercise the stack on real (gitignored) data: `examples/` plus the commodity demo runner. The only layer that touches real data directly.
