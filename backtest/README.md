# backtest/ — Backtest layer

The generic strategy engine + registry, the commodity strategy suite, the signal-augmented-path harness, walk-forward, scoring, and cross-commodity robustness. Drives execution/ (today: a flat cost model).
