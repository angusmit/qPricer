# config/ — Configuration layer (reserved, ARCHITECTURE.md §2)

Environment configs (`.cfg`, base + {env} overrides) and tabular reference data (calendars, instrument specs). Populated in migration step 2; empty for now.
