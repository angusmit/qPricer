# qFDM — Changelog

Version history, newest first. The current-state summary lives in `CLAUDE.md`; the live version is `.qfdm.version` at the bottom of `core/init.q`.

v0.82 (Post-foundation step R17, ARCHITECTURE.md §11.8 / §13-R8 re-run) is the **first proof the realistic foundation generalises beyond the strategy it was demoed with: re-run R8's `factorRelativeValue` (curve PCA → fade the residual) through the realistic end-to-end path R16 assembled (`.workflow.runReplay`).** R8 honestly found NO tradeable edge on the OLD vectorised, frictionless, FULL-SAMPLE-PCA engine; R17 asks one clean question — *does realistic evidence change the verdict?* **THE POINT: this is a VALIDATION, not a hunt for edge.** The realistic path can only make the bar HARDER (realistic txn costs + a CAUSAL trailing-window PCA + deflation against the trial count + the sealed one-shot holdout all SUBTRACT — a frictionless full-sample backtest is the easy case), so the honest expectation is that realistic evidence CONFIRMS or STRENGTHENS the no-edge finding — and that is the SUCCESS case (it proves the foundation generalises and the old verdict was not a fluke of the old engine). **DO NOT TUNE:** the SAME pre-registered params (`k` / lookback / residual-fade thresholds) are read from `.cfg.factor` + `.cfg.templates.rv` — NO new tuned config, no parameter sweep, run ONCE. ADDITIVE — a thin replay-mode RUNNER that REUSES R8's signal; it changes no existing compute path and leaves R8's `factorRelativeValue` template, its factor-stability + residual-stationarity gates, its old demo, and its tests byte-for-byte UNTOUCHED (the historical record of the old-engine verdict), and leaves `.workflow.runReplay` and `gov/` UNCHANGED. (1) **The replay-mode runner** (`templates/factor_rv_replay.q`, `.template.factorRvReplay.*`): per R12 step it computes R8's signal CAUSALLY — `.factor.pca` on a TRAILING `lookback` window of as-of curve changes ONLY (the only look-ahead in R8 was its FULL-SAMPLE PCA; the fix is a causal window, NEVER the full sample), then R8's exact residual-fade rule (cumulative residual → causal z → fade extremes, reusing `.template.rv.__rollMean`/`__rollSd`); takes the position on the ACTUAL contract the curve names at the `residualMaturity` rank (the 3rd-nearest — comfortably live, expiry≫asOf, rolls FORWARD as the front rolls off, so R13's universe + rollRespected pass); books its OWN price change (difference method, 0 on a roll — no roll gap) NET of `.exec.fill` cost; and emits an R12-shape run record so R13's audit + R15's attribution consume it. It is deliberately NOT registered as a new R2 template and adds NO new card — it REUSES R8's `factorRelativeValue` card (risk memory `covid2020`) for gating, so no new audit surface. (2) **End-to-end through `.workflow.runReplay`** (R16, UNCHANGED): pre-register (R3, R8's params) → carded gating on R8's card (R5) → REPLAY (R12) → `.evidence.gatedRun` (R13: audit → on-PASS `.gov.runFull`'s cost → deflated-Sharpe → walk-forward → sealed-holdout cascade) → regime skeptic → `.attribution.pnl` (R15) → a bounded human-escalation packet. ADDITIVE / byte-identical: R8 / `.workflow.runReplay` / `gov/` are byte-for-byte UNCHANGED, so the existing **415 tests stay byte-identical** (gamma_scalp `totalPnl=0.6521844`, short_variance `premium=11.97994`, crude 63.27->57.68, cross-commodity momentum +0.7403373/0.75, gas carry -0.0243->+0.1773, calendar_roll independentResidual 4.742734e-015 / flowResidual 0 all unchanged) + 1 new synthetic test (`test_factor_rv_replay`, a new `factorReplay` group: the signal is CAUSAL — a planted FUTURE curve move does NOT change a past signal; the run record PASSES the R13 evidence audit — actual contracts + forward rolls + all 8 checks; R8's factor-stability + residual-stationarity gates still BITE — reject rotated loadings / a random walk — and are wired into the run; the loop is BOUNDED — escalateToHuman / no deploy field / never tradeable without all gates; the attribution is computed; a contaminated run → `evidenceFailed`; with GOV + CARDS state isolation — reset `.gov.hypoTbl` + restore `modelCards:.cfg.cards` + a unique hypoId; and R8's template is intact + a DISTINCT function) = **416/0**. NO new config; the runner loads after `factor_relative_value.q`. Demo `apps/examples/factor_rv_replay.q` (real CRUDE, NOT tuned): the evidence audit PASSES; the HONEST verdict CONFIRMS R8's no-edge finding — backwardation (1072 obs) and contango (249) **reject at cost** (net Sharpe 0.03 / −0.18), and the tempting flat bucket (87 obs) is **deflated to research** (DSR 0.61 < 0.95); anyTradeable=0, escalated to the human. **The payoff is the attribution (R15):** even a no-edge strategy gets its PnL explained — level 0.38 / slope −0.11 / curvature −0.19 / carry 0.01 / **residual 1.66 (RESIDUAL FRACTION 0.95)** — the realised PnL is NOT the structural slope/curvature normalisation the factor-RV thesis CLAIMS; it is overwhelmingly unexplained residual (idiosyncratic noise), exactly the red flag deflation + the holdout exist to punish, and exactly what you expect from a strategy that fades the residual. A confirmed no-edge verdict — judged on audited, causal, cost-and-roll-aware evidence and ATTRIBUTED to noise — is the architecture doing its job: the rigorous judge, now handed only evidence it can trust, returns the same honest answer on a real PRIOR strategy as it did on the R16 capstone. Nothing renamed.

v0.81 (Research OS step R16, ARCHITECTURE.md §11.8(h) / §13-R16) adds the **first real research output — seasonally-adjusted crude calendar-spread mean-reversion, run END-TO-END. THE CAPSTONE: the evidence-layer foundation R9–R16 is now COMPLETE.** The first time the whole foundation (R9 door → R10 curve → R11 roll → R12 replay → R13 evidence audit → R14 seasonality/carry → R15 attribution) is exercised on a plausible edge, producing the first real verdict the system was built to deliver. **THE POINT: an HONEST answer, not a passing one** — the parameters are PRE-REGISTERED (`.cfg.strategy.seasonalCalSpread`) and NOT tuned (tuning the legs / z-window / thresholds is exactly where a calendar spread overfits, and the deflation + walk-forward + sealed holdout punish it); a "research, not tradeable" verdict is the system WORKING. ADDITIVE — a new template + a new bounded workflow variant; it changes no existing compute path, leaves `gov/` UNMODIFIED, and leaves the existing `.workflow.run` UNCHANGED. (1) **The `seasonalCalendarSpread` template** (`templates/seasonal_calendar_spread.q`, `.template.scs.*`): fade the crude M1-M3 calendar spread against its SEASONALLY-ADJUSTED same-calendar-month z-score (R14) — SHORT when z>+entry (unusually wide for the season), LONG when z<-entry, EXIT on a z-cross of zero or `maxHold`; trades the ACTUAL legs (skip the expiring contract via a `rollBuffer` — roll discipline, R11/R10); books NET PnL via the fill model (`.exec.fill`); and — unlike R6's relativeValue — emits an R12-shape RUN RECORD so the evidence audit (R13) and the PnL attribution (R15) consume it end-to-end. Edge `structural` (cards vocabulary). Its own template-specific STATIONARITY gate (reuse R6's `.template.rv.stationarity`, AR(1)/OU) BITES: a random-walk spread → fading the z is fading noise → rejected before the universal gates. Composes R9/R10/R11/R14 downward; conforms to the `template` contract (`pnl`/`validation`/`meta`). (2) **The end-to-end replay-mode bounded loop** `.workflow.runReplay` (the composition R13 deferred — "assembled when the first strategy runs"): PRE-REGISTER the hypothesis (R3) → `.cards.gatedRun` carded precondition (R5) → `.evidence.gatedRun` (R13: audit the replay run record; on PASS delegate to `.gov.runFull`'s cost → deflated-Sharpe → walk-forward → sealed-holdout cascade; on FAIL `evidenceFailed`, the gates NEVER run) → the regime risk-memory skeptic → `.attribution.pnl` (R15) → a human-escalation packet. BOUNDED BY CONSTRUCTION (the R7 discipline): always `decision=escalateToHuman` / `humanSignOffRequired=1b` / `stage=paper`, NO deploy/allocate/autoTrade field, NEVER `tradeable` without every gate passing. The existing `.workflow.run` is UNCHANGED (the variant is purely additive); `gov/` is UNMODIFIED (the path reaches the gates via `.evidence.gatedRun` → `.gov.runFull`, both existing). ADDITIVE / byte-identical: `gov/` and `.workflow.run` are byte-for-byte UNCHANGED, so the existing **413 tests stay byte-identical** (gamma_scalp `totalPnl=0.6521844`, short_variance `premium=11.97994`, crude 63.27->57.68, cross-commodity momentum +0.7403373/0.75, gas carry -0.0243->+0.1773, calendar_roll independentResidual 4.742734e-015 / flowResidual 0 all unchanged) + 2 new synthetic tests (`test_seasonal_calendar_spread`: the position state machine — z>+thr SHORTs / z<-thr LONGs / exit on z-cross / maxHold; the causal seasonal z uses only same-calendar-month obs before t; the stationarity gate passes a mean-reverting spread and rejects a random walk; `test_workflow_runreplay`: the loop is BOUNDED — always escalateToHuman, no deploy field, never tradeable without all gates; the EVIDENCE AUDIT is invoked — a contaminated replay run → `evidenceFailed` with the gates never run; the ATTRIBUTION is computed; with GOV + CARDS state isolation — reset `.gov.hypoTbl` + restore `modelCards:.cfg.cards` + unique hypoIds, the R13 caveat that the runner values every test in one process) = **415/0**. New `.cfg.strategy.seasonalCalSpread` (PRE-REGISTERED params: deferredIdx / minObs / entryThreshold / maxHold / txnCostRate / notional / maxAr1 / rollBuffer — FIXED, not swept); the template loads after the other templates, `.workflow.runReplay` is added alongside `.workflow.run`. Registered as a `template` kind (`seasonalCalSpread`, passing `.contracts.verify`); carded (R5) WITH a populated failure-mode field (risk memory `energyShock2022`) so it is gateable. Demo `apps/examples/seasonal_calendar_spread.q` (real CRUDE, NOT tuned): the evidence audit PASSES, then the HONEST verdict — backwardation (1105 obs) and contango (249) **reject at cost** (net Sharpe 0.24 / −0.06 below the 0.25 hurdle), and the tempting flat bucket (138 obs, in-sample Sharpe 2.6) is **deflated to research** (DSR 0.86 < 0.95 — the system refusing to be fooled by a small-sample slice); anyTradeable=0, escalated to the human, nothing deployed. **q trap recorded (skill #26):** the test runner `value`s every test in ONE process, so globals persist — reset `.gov.hypoTbl` + restore `modelCards:.cfg.cards` + use unique hypoIds; also the backtick MULTI-index `X`a`b` = `X[`a`b]` (a 2-list), not `X[`a][`b]`. A not-tradeable verdict here is not a failure — it is the architecture doing exactly what it was built to do: tell the truth instead of a flattering backtest. **With R16, the evidence-layer foundation R9–R16 is COMPLETE**; everything after — re-running factor-RV (R8) on this foundation, then the deferred strategies and pricing extensions — rides on a stack that is realistic end-to-end. Nothing renamed.

v0.80 (Research OS step R15, ARCHITECTURE.md §11.8(g) / §13-R15) adds **PnL explain + bucketed curve risk** — turning "explain why this makes money" from prose into a QUANTITATIVE attribution. Given a replay run record (R12) + the curve (R10), decompose the realized PnL into commodity-market components — level / slope / curvature / carry / residual — and compute the bucketed curve risk on the positions. This is the quantitative form of Sec 10's "name which of the three edges": mostly CARRY = a harvested risk premium; mostly SLOPE/curvature = a structural relative-value normalisation; a large RESIDUAL = unexplained (a red flag for noise / overfit). **THE LAYERING CORRECTION:** §11.8(g) previously said the attribution "extends `analytics/`" — that is WRONG given the dependency rule (`analytics/` sits BELOW `backtest/`, but the attribution must CONSUME a replay run record, which lives in `backtest/`, so placing it in `analytics/` would be an illegal UPWARD dependency); it is corrected to a NEW HIGH layer `attribution/` ABOVE `backtest/` (the same kind of placement fix as R10's curve-snapshots-storage correction). ADDITIVE — a new capability that READS a run record + the curve and computes; it changes no existing compute path, does NOT touch the existing `analytics/` functions, and does NOT rewire `cards/`. New `attribution/attribution.q` (`.attribution.*`): reads `backtest/` (the run record — a data structure), `curve/` (R10's curve + the parallel/slope/butterfly shock operators — the decomposition BASIS — and `rollYield`), and `state/` (R9) — all downward; loads after the backtest block (after `replay.q`); opens nothing at import. (1) **PnL decomposition** `.attribution.pnl[run]` → `` `level`slope`curvature`carry`residual`total`residualFraction`reconciled ``: per step the active contract's price change `P_t(τ_t)−P_{t-1}(τ_{t-1})` splits into the CARRY / roll-down (`P_t(τ_t)−P_t(τ_{t-1})` — the contract sliding along the CURRENT curve as it ages) + the curve SHIFT at the fixed tenor (`P_t(τ_{t-1})−P_{t-1}(τ_{t-1})` — interpolate the current curve at the prior tenors minus the prior prices), and the shift is projected onto R10's parallel/slope/butterfly shapes (derived FROM the shock operators, not reinvented) via the normal equations → level/slope/curvature; whatever the basis does not span is the curve residual. `residual` is DEFINED as `realizedStepPnL − (level+slope+curvature+carry)`, so it also absorbs costs/financing and the five ALWAYS reconcile to the realized total (like R13's `pnlTies`); the `residualFraction` (`|Σresidual|/|total|`) is the quantitative "name which edge, or it's noise." (2) **Bucketed curve risk** `.attribution.risk[run]` → the bucketed curve `delta` per tenor (the end position's $ sensitivity to a unit move per maturity bucket), the `rollDownExposure` (position × the end curve's `rollYield`), and the `calendarSpreadExposure`. CAREFUL POINTS: the decomposition RECONCILES by construction (the residual is the plug — the non-trivial tests are the KNOWN-ANSWERS); R10's level/slope/curvature shocks pivot on the FRONT, so a front-only position shows ~0 slope/curvature exposure (the tests isolate slope/curvature on a DEFERRED position); a large residual is the "name which edge" red flag. ADDITIVE / byte-identical: `analytics/`, `cards/`, gov/, workflow/ are byte-for-byte UNCHANGED, so the existing **412 tests stay byte-identical** (gamma_scalp `totalPnl=0.6521844`, short_variance `premium=11.97994`, crude 63.27->57.68, cross-commodity momentum +0.7403373/0.75, gas carry -0.0243->+0.1773, calendar_roll independentResidual 4.742734e-015 / flowResidual 0 all unchanged) + 1 new synthetic attribution test (`test_attribution`: known-answers — a pure PARALLEL move attributes to level with residual~0; a pure SLOPE move on a deferred position → slope dominant; a tenor-static backwardated curve aged 30 days → carry dominant; THE RECONCILIATION level+slope+curvature+carry+residual == realized within tol; an out-of-basis zigzag move → a HIGH residual fraction; the bucketed risk → the front-bucket delta + a positive roll-down on backwardation) = **413/0**. New `.cfg.attribution` (`reconcileTol` + tenor `buckets`); `attribution/attribution.q` loads in `core/init.q` after `replay.q`. Registered as a new R2 `attribution` kind (`pnlAttribution`, passing `.contracts.verify`); carded (R5, edge `na — it explains a run, it does not trade) with real failure modes (the residual lumps everything outside the basis — a basis-spread / inter-commodity move lands in residual, so a large residual is "unexplained", noise OR a real unmodelled factor; the carry attribution depends on R10's `rollYield`; the decomposition assumes the curve basis spans the relevant moves). The attribution feeds the model card's economic-rationale with numbers — but auto-populating a card from it is a CONSUMER concern (R16's strategy card), so `cards/` is NOT rewired here (the capability is provided + documented). Demo `apps/examples/pnl_attribution.q` (real CRUDE momentum: level 0.119 / slope -0.032 / curvature 0.062 / carry 0.011 / residual 0.0002, reconciled, residual fraction 0.12% — the PnL is well-explained by curve factors + carry; the bucketed front-bucket delta + a +0.98 roll-down exposure). **q trap** (already in the skill, #24/#3): the backtick-index precedence `(t)`price - px` parses as `t[`price - px]` — write `((t)`price)-px`. With R15 in, the first real strategy (R16) is judged not only on whether it survives the gates but on WHETHER ITS PnL IS EXPLAINED — the difference between a strategy a desk can stand behind and a curve-fit that cleared the bar. Nothing renamed.

v0.79 (Research OS step R14, ARCHITECTURE.md §11.8(f) / §13-R14) adds the **seasonality + carry FEATURE capabilities** — the commodity features the first real strategy (R16) is built on. The seasonally-adjusted same-calendar-month z-score IS the signal for R16's calendar-spread mean-reversion; the carry signal is the basis for the carry strategies. Both are computed CAUSALLY through R9's door from R10's curve, so the features are point-in-time by construction. ADDITIVE — two new capabilities that READ the curve + as-of data and compute features; they change no existing compute path, and do NOT rewire `.state.build` or touch the existing `signals/` seasonality alpha-library (`.commodity.seasonality.*`, distinct). New `season/season.q` (`.season.*`) + `carry/carry.q` (`.carry.*`), LOW evidence-tier layers: load AFTER `curve/` (reuse `.curve.build`'s output/convention), read `state/` (R9) downward, open nothing at import. (1) **Seasonality** `.season.features[asOf;comm]` → `sameMonthZ` (the z of the front-deferred spread vs its SAME-CALENDAR-MONTH historical distribution up to `asOf` — the seasonally-adjusted z, the R16 signal), `sameContractMonthZ` (vs the same front delivery-month historically), `seasonalFactor` (the calendar-month mean front level, causal), `deseasonalised` (front level ÷ seasonal factor), `seasonalSlope` (the slope minus its same-month mean), + `nSameMonth`/`lowConfidence`. **THE KEY POINT-IN-TIME DISCIPLINE:** the same-month z uses ONLY same-calendar-month observations UP TO `asOf` (trailing/causal, obtained through R9's door — `.state.asof` returns `date<=asOf`), NEVER the full sample — a full-sample seasonal statistic is look-ahead (it standardises a past observation by future same-months, the silent killer of seasonal backtests). Below a minimum same-month N (`.cfg.season.minObs`) the z is null + `lowConfidence` rather than a spurious value (thin same-month history early in the data). The causal per-date spread series is built in ONE pass with plain vector ops (mask + `where` + `group`) — NOT qSQL on the local slice (which throws `'assign`); the calendar month is `(`month$date) mod 12` (the `.mm` accessor is atom-only). (2) **Carry** `.carry.features[asOf;comm]` → `impliedCarry` (the curve-implied carry/roll = R10's annualised `rollYield` = `ln(Ffront/Fdeferred)/T`, POSITIVE in backwardation / negative in contango), `convenienceYield` (`(r+storage)−impliedCarry`), `fairValueCarry` (the cash-and-carry no-arb deferred price `Ffront·exp((r+storage)·T)`, with `actualDeferred` to compare), `carrySignal` (`impliedCarry−(r+storage)` = the roll yield net of the assumed cost of carry, the edge signal, flips sign with the curve), `inventoryTightness` (a PROXY from the degree of backwardation). **HONEST DATA SCOPE:** the implied carry comes from the curve (we have it); the convenience yield + cash-and-carry fair value need a rate `r` + storage cost SUPPLIED from `.cfg.carry` (config, NOT market-observed) — so those outputs are ASSUMPTION-DEPENDENT and flagged in `assumptionDependent`; the inventory-tightness is a PROXY (we have NO real inventory data — never fabricated); cash-and-carry assumes storability (the same discipline as R11's no-open-interest). ADDITIVE / byte-identical: `.state.build`, the `signals/` seasonality, `gov/` and `workflow/` are byte-for-byte UNCHANGED, so the existing **410 tests stay byte-identical** (gamma_scalp `totalPnl=0.6521844`, short_variance `premium=11.97994`, crude 63.27->57.68, cross-commodity momentum +0.7403373/0.75, gas carry -0.0243->+0.1773, calendar_roll independentResidual 4.742734e-015 / flowResidual 0 all unchanged) + 2 new synthetic tests (`test_seasonality`: the same-month z known-answer; THE KEY INVARIANT — a planted FUTURE same-month observation does NOT change a past z (causal); below min-N the z is null + lowConfidence; a constant per-calendar-month level deseasonalises to 1; `test_carry`: implied carry positive on a backwardated curve / negative on contango, convenience yield = `(r+storage)−carry` and cash-and-carry fair value as known-answers, the carry signal flips sign with the curve, the inventory-tightness proxy tracks the backwardation degree) = **412/0**. New `.cfg.season` (`deferredIdx` = the seasonal spread's deferred leg, default 2 = M1-M3; `minObs`) + `.cfg.carry` (`riskFreeRate`, `storageCost`); both layers load in `core/init.q` after `curve/`. Registered as new R2 `season` + `carry` kinds (`curveSeasonality` / `carryEconomics`, both passing `.contracts.verify`); carded (R5) with real failure modes (seasonality: thin same-month history early → noisy z mitigated by min-N, seasonal patterns break in structural shifts, causal stats use less data than full-sample; carry: convenience yield / fair value depend on the assumed `r`+storage, inventory-tightness a proxy, cash-and-carry assumes storability). Demo `apps/examples/season_carry.q` (real CRUDE: the causal same-calendar-month z (168 obs, all dated ≤ asOf) vs the full-sample look-ahead contrast; the implied carry / convenience yield / carry signal on a backwardated 2023 date (carry +0.023) vs a contango 2020 date (carry −0.084) — the signal flips sign). **q traps recorded** (skill): the `.mm` accessor is atom-only (use `(`month$date) mod 12` for a date vector); `sv` is a reserved builtin (scalar-from-vector) — not a usable local name. With R14 in, R16 has its signal and every strategy can attribute its return to carry / seasonality / normalisation. Nothing renamed.

v0.78 (Research OS step R13, ARCHITECTURE.md §11.8(e) / §13-R13) adds the **Evidence Audit** — the evidence-layer GATE that closes the loop the door opened. R9's accessor PREVENTS look-ahead by construction; R12's replay RECORDS the provenance (every step's as-of data access, the book, the fills, the rolls, the PnL components); R13 VERIFIES it — a DETERMINISTIC pass/fail audit that bites on a replay run record BEFORE the governance gates ever see its PnL, wired exactly like carded gating (a HARD precondition: FAIL → reject, the gates never run, gov UNMODIFIED). After R13, the only PnL the gates judge is PnL PROVEN to correspond to something a desk could actually have traded. ADDITIVE — a new verifier + a thin precondition wrapper; it changes no existing compute path, leaves `gov/` UNMODIFIED, and leaves the bounded workflow (`.workflow.run`, R7) UNCHANGED. New `evidence/evidence.q` (`.evidence.*`, a layer ABOVE `backtest/` — it consumes R12's run record — and BELOW `gov/` — it reads the zone CONFIG `.cfg.gov.zones`, NOT gov's logic; reads `state/`/`backtest/`/`config/` downward; loads after `backtest/`, before `gov/`; opens nothing at import). (1) **The deterministic verifier** `.evidence.audit[run]` → `` `pass`checks`reason `` (pure, no RNG), with EIGHT checks each derivable from the R12 run record (+ state/config): **lookAhead** (every step's `asOf` is its own date and no as-of slice saw data dated after `asOf` — `provDateTo<=asOf`; R9 prevents it, the audit verifies it and catches any path that bypassed the door), **universe** (every fill's contract was live — expiry`>`asOf — as of its date), **rollRespected** (the recorded `rollEvents` are EXACTLY the transitions in the active-contract series — no fabricated/missing roll — and every roll is forward in contract; the roll ENGINE is R11, this verifies the recorded trail is rule-consistent), **costsApplied** (a run with fills carries a positive transaction cost unless `requireCosts` off), **fillsPresent** (no position change without a corresponding fill), **bookTies** (the position book equals the cumulative fills), **pnlTies** (`stepPnl ≈ positionPnl − totalCost − financingCost` per step within `.cfg.evidence.reconcileTol` — the "is this PnL real" reconciliation), **containment** (the run's dates ⊆ train+validate per `.cfg.gov.zones` — a LIGHT cross-check computed from the zone CONFIG's floor-cut + the date axis; the holdout SEAL stays in governance §11.4 — the audit verifies, it does NOT re-implement). Any check false ⇒ `pass=0b` with `reason` naming the failed checks. (2) **The fail-safe precondition wrapper** `.evidence.gatedRun[hypoId;run;runner;axis]` MIRRORS `.cards.gatedRun`: on PASS delegate to the gates (`.gov.runFull`); on FAIL return a reject verdict `evidenceFailed` (`tradeable=0b`) and the gates NEVER run (gov is not invoked). A run that cannot be PROVEN clean is rejected, not judged — mirroring R3b's fail-safe verdicts. **THE AUDIT BITES on every check** (an audit that always passes manufactures false confidence): `tests/evidence/test_audit_bites.q` feeds one deliberately-contaminated run PER check and confirms the audit catches it (a planted `provDateTo>asOf` → lookAhead fails; a fabricated-contract fill → universe; a fabricated roll event → rollRespected; zeroed costs → costsApplied; a position change with no fill → fillsPresent; a book offset from fills → bookTies; a non-tying stepPnl → pnlTies; a holdout-spanning range → containment). INFRASTRUCTURE (a gate, like the gov gates + the replay engine) — deliberately NOT registered as an R2 capability and NOT carded. ADDITIVE / byte-identical: `gov/` and `.workflow.run` are byte-for-byte UNCHANGED, so the existing **407 tests stay byte-identical** (gamma_scalp `totalPnl=0.6521844`, short_variance `premium=11.97994`, crude 63.27->57.68, cross-commodity momentum +0.7403373/0.75, gas carry -0.0243->+0.1773, calendar_roll independentResidual 4.742734e-015 / flowResidual 0 all unchanged) + 3 new synthetic evidence tests (`test_audit_passes`: a clean replay run passes all 8 checks; `test_audit_bites`: one contaminated run per check, each caught with the right check failing; `test_gated_run`: a failed audit yields `evidenceFailed` with the gates NOT run, a passed audit delegates to the gates, and the audit stays isolated from shared gov state) = **410/0**. New `.cfg.evidence` (`reconcileTol` + `requireCosts`); `evidence/evidence.q` loads in `core/init.q` after the backtest block, before `gov/`. Demo `apps/examples/evidence_audit.q` (real CRUDE: a clean replay run inside train+validate passes all 8 checks; the SAME run with a planted look-ahead + a non-tying PnL fails lookAhead + pnlTies; `.evidence.gatedRun` REJECTS the contaminated run with `evidenceFailed` and the gates do not run, while the clean run is delegated to the gates). **q trap recorded** (skill caution #24): qSQL `select`/`exec`/`update` on a LOCAL table inside a lambda throws `'assign` — the audit filters with plain vector ops (`steps where mask`) instead. The door (R9) prevents look-ahead, the replay (R12) records it, the audit (R13) PROVES it — a contaminated backtest is rejected BEFORE the gates ever deflate a fabricated Sharpe, so the first real strategy (R16) and the re-run of factor-RV (R8) will be judged on PnL proven realistic. Nothing renamed.

v0.77 (Research OS step R12, ARCHITECTURE.md §11.8(d) / §13-R12) adds the **event-driven replay engine** — THE KEYSTONE of the evidence layer. R9 gave the single as-of door, R10 the curve, R11 roll discipline — all as-of-clean inputs; R12 folds them into a DETERMINISTIC date-by-date loop that produces REALISTIC, cost-and-roll-aware PnL on the ACTUAL contracts, and emits the auditable run record R13's evidence audit will bite on. After R12, replay mode is the promotion gate: every verdict the gates produce finally corresponds to something a desk could have traded. **THE SINGLE LOUDEST REQUIREMENT — byte-identity:** the replay is a NEW path that REUSES the UNCHANGED `.exec.fill` and the UNCHANGED research-mode backtester; the realism levers (`.cfg.replay`) are all OFF by default, so a frictionless replay calls the unchanged `.exec.fill` and every canonical stays byte-identical. New `backtest/replay.q` (`.backtest.replay.*`, a HIGH layer within `backtest/`: composes `state/` (R9) + `curve/` (R10) + `roll/` (R11) + `execution/` + the signal path, all downward; loads after commodityStrategies.q; opens nothing at import). (1) **The deterministic fold** `.backtest.replay.run[strategy;comm;fromDate;toDate;cfg]` → an auditable run record `` `meta`steps`rollEvents`provenance ``: the loop is `step\[seed; rows]` — a pure `step[state;row] -> state'` folded over the sorted trading dates (no RNG, so two runs are byte-identical). Per step it builds the as-of slice through R9's door (logging provenance), decides the active contract **as of the PRIOR date** (R11's `days_before_expiry` engine with `W=1` — the causally-honest convention, and the one the research path uses: its `heldSeq` roll scan is itself one-day-lagged, so deciding the roll on the prior date makes the replay's active track `heldSeq` EXACTLY), re-derives the as-of return on that contract, generates the order (target − current + carried remainder), fills via the unchanged `.exec.fill` (+ opt-in realism), and books PnL (price − cost − financing). (2) **Faithfulness — the look-ahead detector** `.backtest.replay.faithfulness[strategy;comm;tol]`: the replay REUSES the causal signal→target (the unchanged strategy research run — the R6 faithful-delegation precedent) and INDEPENDENTLY re-derives the as-of roll + return + fill + book + PnL, so a frictionless replay reproduces the research-mode PnL **byte-for-byte on every date where the roll map names the same contract** (real CRUDE momentum: research 0.3175637 == replay 0.3175637, relDiff 0); a divergence there is look-ahead or a roll/fill bug — the replay caught it. It is a tolerance not an equality by design (the replay re-derives returns from the as-of slice independently of the path builder's vectorised construction, so float rounding order can differ). The few trailing DATA-EDGE dates (the research path holds a contract that has stopped trading at the tail of the data, while the replay rolls to the only listed contract) are reported SEPARATELY as `nEdge`/`edgeDates` — not hidden, not a look-ahead. (3) **The execution-realism extension** — NEW additive functions in `execution/execution.q`, NOT edits to `.exec.fill`: `.exec.participationCap[order;barVolume;rate]` (split a signed order into filled+remainder, capping the fill at `rate*barVolume`; remainder carries/drops per `.cfg.replay`) and `.exec.rollPenalty[rolledNotional;isRoll;penaltyBps]` (the difference-method return treats a roll as costless, so the realistic roll cost is charged on the rolled exposure = |position value|*penaltyBps). Default off/zero → the frictionless replay calls the unchanged `.exec.fill` and the §4 `deltaPV==stepPnl` invariant holds. (4) **The auditable run record** holds per-step book / fills (date/contract/qty/price/cost components) / roll events / the per-step PnL components (price/cost/financing) + the per-step `asOf` provenance cross-referencing R9's append-only `.state.provLog` (`provDateTo<=asOf` always — the no-look-ahead guarantee R13's audit verifies); persisted opt-in via `.backtest.replay.persist`/`open` to the splayed, **UNPARTITIONED** `replayRuns` (§3 storage: `p#`/`.Q.en`/`set`, gitignored, tests synthetic). ENGINE INFRASTRUCTURE (like the research-mode backtester) — deliberately NOT registered as an R2 capability and NOT carded (capabilities/templates/strategies get registered + carded; the runner does not). ADDITIVE / byte-identical: `.exec.fill` and the research-mode engine (`strategy.q`/`commodityStrategies.q`) are byte-for-byte UNCHANGED, so the existing **405 tests stay byte-identical** (gamma_scalp `totalPnl=0.6521844`, short_variance `premium=11.97994`, crude 63.27->57.68, cross-commodity momentum +0.7403373/0.75, gas carry -0.0243->+0.1773, calendar_roll independentResidual 4.742734e-015 / flowResidual 0 all unchanged) + 2 new synthetic replay tests (`test_replay_loop`: the fold is deterministic, a frictionless replay of a constant-long synthetic futures strategy reproduces an INDEPENDENT vectorised total to 1e-10, the run record's PnL ties to positions×price-moves+costs (the R13 reconciliation), the book ties to the fills, and NO step used data with date>asOf; `test_replay_execution`: the participation cap clips a fill to `rate*barVolume`, the roll penalty charges the rolled exposure once at the roll and net degrades by exactly that, and with realism OFF the fills equal the unchanged `.exec.fill` output) = **407/0**. New `.cfg.replay` (participationRate / remainderPolicy / rollPenaltyBps / financingRate / annualizationDays / rollW — all off/zero/identity by default); `replay.q` loads in the backtest block after commodityStrategies.q. Demo `apps/examples/replay_engine.q` (real CRUDE: (1) faithfulness PASS byte-for-byte; (2) realism ON — participation cap + roll penalty — degrades net PnL gross 0.088 → net −0.100, cost drag 0.188 over 13 rolls; (3) the run-record slice: roll events, book+fills+per-step PnL components, the as-of provenance). Replay mode is now the promotion gate; R13's evidence audit consumes the run record (the door prevents look-ahead, the replay records, the audit proves). Also folded the recurring q traps + the background-job-polling ops lesson into `.claude/skills/q-pricing/SKILL.md` + CLAUDE.md (docs-only). Nothing renamed.

v0.76 (Research OS step R11, ARCHITECTURE.md §11.8(c) / §13-R11) adds **roll discipline** — the THIRD evidence-layer milestone. Commodity backtests are most often wrong for one of two reasons: rolling on data the strategy could not have seen, or backtesting an abstract continuous series instead of the contracts you would actually trade. R11 makes roll discipline a **config + a single function**: which contract is ACTIVE as of a date, decided DETERMINISTICALLY FROM AS-OF DATA ONLY (through R9's door), with roll events recorded and the continuous series demoted to an analytics-only derived view. **THE PRINCIPLE: trade the actual contracts the roll map names; never trade or signal off the continuous series.** ADDITIVE — a new capability that READS the as-of data through R9's door and computes; it changes no existing compute path and does NOT rewire the existing readers. New `roll/roll.q` (`.roll.*`, a LOW layer: depends ONLY on R9's door `.state.asof`, independent of `curve/` (placed after it for tidiness); reads `state/` (and via it `data/`) downward only; opens nothing at import). (1) **The rule engine** `.roll.active[asOf;comm]` → the active contract(s) as of `asOf`, decided from the door's `date<=asOf` output ONLY: `active` (the primary `contractYM`), `weights` (`contractYM!weight` — one contract normally, two during a roll window), `reason`, plus `asOf`/`commodity`/`effectiveDate`/`ruleType`. Rules live in `.cfg.rolls` (per-commodity with a `default` fallback): `days_before_expiry` (DEFAULT — hold the nearest-expiry contract whose `expiry-asOf > rollDays`, and in the `W` days before that contract's own roll point BLEND held→next so the roll is not a single-day jump), `volume_switch` (front vs next by trailing as-of volume; roll when the next's volume crosses the front's), `fixed_calendar` (roll on/after the `rollDays`-th of the front's expiry month); `oi_switch` is DESIGNED but FLAGGED UNAVAILABLE — the HDB carries volume + expiry but **NO open interest**, so requesting it ERRORS rather than fabricating OI (the same honesty as R9's revision-awareness without fabricated revision columns). (2) **Roll events** `.roll.events[comm;fromDate;toDate]` walks the dates and emits an `([] date; commodity; fromContract; toContract; reason)` row each time the active contract changes (dates with no live contract — the data edge — are dropped). `.roll.eventsPersist`/`.roll.eventsOpen` store the splayed, **UNPARTITIONED** `rollEvents` table — the SAME storage decision as the HDB (§3): `p#` on `commodity`, sym columns via `.Q.en`, written with `set`, NOT date-partitioned; the dir is gitignored, tests synthetic-only. (3) **The analytics-only continuous view** `.roll.continuous[comm]` → `([] date; contract; rawFront; adjusted; rolled)` — a DERIVED back-adjusted series spliced at each roll using the NEW contract's own move (difference method) so it has no gap, BUT back-adjustment rewrites historical levels at every roll, so the series is **NON-POINT-IN-TIME** and is for charting / long-run vol ONLY — never trade or signal off it (R13 will flag any strategy that does); the tradable, point-in-time path is the actual contract `.roll.active` names. (The active-contract CHOICE per date is still as-of-clean; only the price back-adjustment is retrospective.) THE THREE CAREFUL-REVIEWER POINTS, all enforced: (i) **as-of-only by construction** — every rule consumes ONLY `.state.asof`'s `date<=asOf` output, so a roll decision cannot see future data, and a synthetic test plants a FUTURE volume spike (`date>asOf`) and asserts the past `volume_switch` decision is UNCHANGED (point-in-time safety inherited from the door, not re-implemented); (ii) **honest data scope** — `oi_switch` is flagged unavailable, not faked; (iii) the continuous series is explicitly **labeled analytics-only / non-point-in-time**. ADDITIVE / byte-identical: roll/ is new and reads-via-the-door + computes (never edits), so the existing **403 tests stay byte-identical** (gamma_scalp `totalPnl=0.6521844`, short_variance `premium=11.97994`, crude 63.27->57.68, cross-commodity momentum +0.7403373/0.75, gas carry -0.0243->+0.1773, calendar_roll independentResidual 4.742734e-015 all unchanged) + 2 new synthetic roll tests (`test_roll_active`: `days_before_expiry` holds the right contract / blends old→next across the window (0.6/0.4 at 13d-to-expiry) / rolls exactly at rollDays, THE KEY INVARIANT a planted future volume spike does not change a past `volume_switch` decision, `volume_switch` rolls when as-of volume crosses, `oi_switch` flags unavailable; `test_roll_events_continuous`: exactly one roll event at the right date/from/to for a 2-contract schedule, the `rollEvents` table is a single flat unpartitioned table, the continuous-adjusted series has NO gap at the roll — it moves by the NEW contract's own daily move, not the contract-switch jump) = **405/0**. Registered as a new R2 `roll` kind (`rollEngine`, passing `.contracts.verify`); carded (R5, `card_rollEngine`) with real failure modes (oi_switch unavailable on this data; the continuous series being mistaken for tradable; data-edge expiry-day artifacts). New `.cfg.rolls` (per-commodity type/rollDays/W/priceSource/method); `roll/` loads immediately after `curve/`. Demo `apps/examples/roll_discipline.q` (real CRUDE: the active contract as of a recent date from as-of data only; a clean monthly roll-event cadence over the last year of the clean curve range; the analytics-only back-adjusted continuous vs the actual tradable front price; and the as-of-only property — a past decision is fixed by the data knowable then). It is the contract-selection source the replay engine (R12 — which trades the contracts the roll map names, fills them with realistic costs, and folds over the dates) and the first real strategy (R16) build on. Nothing renamed.

v0.75 (Research OS step R10, ARCHITECTURE.md §11.8(b) / §13-R10) adds the **curve engine** — the SECOND evidence-layer milestone. R9 gave the single as-of door + the Market State (whose `curve` is the raw term structure and whose `features` is an empty placeholder); R10 fills the gap with a dedicated curve capability that consumes the door and computes the RICH derived curve, plus the curve-shock operators and immutable daily snapshots. ADDITIVE — a new capability that READS the as-of data through R9's door and computes derived features; it changes no existing compute path and does NOT rewire `regime/` or `.state.build` (regime/ keeps its own slope/classification derivation; rebasing it onto the curve engine, and filling the Market State's `features`, are later deferrals — same precedent as R9 leaving the direct readers alone). New `curve/curve.q` (`.curve.*`, a LOW layer: loads after `state/`, before the consumers; reads `state/` (and via it `data/`) downward only; opens nothing at import). (1) **The engine** `.curve.build[asOf;comm]` obtains the as-of curve THROUGH R9's door (reusing the SAME raw curve shape regime/ sees), applies a liquidity filter (drop non-positive-price tenors + contracts below the `.cfg.curve` volume floor), and returns a curve object: the clean `curve`, `spreads` (adjacent calendar spreads), and the derived `features` — `slope` = `(deferred-front)/front`, `classification` (`backwardation`/`contango`/`flat`), `curvature` = the butterfly second difference (`near - 2·mid + far`), `rollYield` = annualised `ln(Ffront/Fdeferred)` (positive in backwardation). Deterministic. (2) **The shock operators** `.curve.shock.parallel|slope|butterfly[curve;size]` (pure): add a constant / a front-pivot tenor-proportional amount / a `4x(1-x)` belly bow — clean reusable primitives for the R15 scenario / bucketed-risk work. (3) **Immutable daily snapshots** `.curve.snapshot[asOf;comm]` persist to the splayed, **UNPARTITIONED** `curveSnapshots` table — the SAME storage decision as the HDB (§3): `p#` on `commodity`, sym columns via `.Q.en`, written with `set`, NOT date-partitioned (this corrects the earlier "partitioned by date" wording in §11.8(b)/R10). Immutable: a (commodity, date) snapshot is written once and never overwritten; recomputing must produce the identical curve (deterministic), so a re-snapshot is a no-op-if-matching and a DIFFERING re-write ERRORS (catching non-determinism). `.curve.snapshotAt`/`.snapshotHistory` read; `.curve.snapshotPersist`/`.snapshotOpen` set/get the splay; the dir is gitignored, tests synthetic-only. AGREEMENT with regime/ (the careful-reviewer point): the slope + classification use the SAME convention/threshold as regime/ — `.cfg.curve`'s `deferredIdx` and `flatThreshold` are SOURCED FROM `.cfg.regime` so they cannot drift — and a synthetic test asserts `.curve.__features`'s classification equals `regime/`'s (`.regime.__labelPanel`) on the same curve (so the two never disagree and regime/ can be rebased later with no surprise). ADDITIVE / byte-identical: curve/ is new and reads-via-the-door + computes (never edits), regime/ and `.state.build` are NOT rewired, so the existing **401 tests stay byte-identical** (gamma_scalp `totalPnl=0.6521844`, short_variance `premium=11.97994`, crude 63.27->57.68, cross-commodity momentum +0.7403373/0.75, gas carry -0.0243->+0.1773 all unchanged) + 3 new synthetic curve tests (`test_curve_engine`: backwardation/contango/flat classification + slope/roll-yield sign, the butterfly-curvature known-answer, the three shock operators' known shifts, the regime AGREEMENT cross-check, determinism; `test_curve_snapshot`: write/read-back identity, a matching re-snapshot is a no-op, a differing re-write is rejected (immutability), the table is a single flat unpartitioned table) = **403/0**. Registered as a new R2 `curve` kind (`curveEngine`, passing `.contracts.verify`); carded (R5) with real failure modes (thin-liquidity tenors distorting slope/curvature; classification threshold sensitivity near the flat boundary; regime/ not yet rebased so two derivations coexist - mitigated by the agreement test; snapshot immutability assumes a deterministic curve). New `.cfg.curve` (deferredIdx + flatThreshold sourced from `.cfg.regime`; minVolume); `curve/` loads immediately after `state/`. Demo `apps/examples/curve_engine.q` (real CRUDE: a backwardated date 2019-04-18 (slope -0.018, roll +0.074) vs a deep-contango date 2020-03-19 (slope +0.186, roll -0.41) - the classification flips; the three shocks; 5 immutable snapshots round-tripped). It is the curve source the rest of the evidence layer composes (replay reads curves per date, season/carry derive features from it, PnL attribution decomposes along its slope/curvature/butterfly axes, the first real strategy lives on its spreads + classification); the immutable snapshots make a replayed curve reproducible rather than recomputed. Nothing renamed.

v0.74 (Research OS step R9, ARCHITECTURE.md §11.8 / §13-R9) adds **point-in-time market state + as-of discipline** — the FIRST milestone of the evidence layer and the foundation the rest of it (R10–R16) stands on. R1–R8 built a rigorous JUDGE (governance, deflated Sharpe, sealed holdout, the bounded workflow); the evidence layer is the realistic evidence the judge is handed, and *a rigorous judge is only as honest as the evidence*. ADDITIVE — a new accessor + a state object that READ the existing HDB through the existing query layer and compose existing data; it changes no existing compute path and does NOT rewire the existing direct readers (regime/, backtest/ keep their current HDB access — rebasing them is a later deferral). New `state/state.q` (`.state.*`, a LOW layer: loads after `data/`, before the consumers; wraps the UNCHANGED `.data.hdb.*` query layer; opens no HDB at import). (1) **The single as-of accessor** `.state.asof[asOf;comm]` → `` `data`provenance `` — the ONLY door to history for foundation code: returns ONLY rows knowable as of `asOf` (`date<=asOf`) via the existing layer, and is REVISION-AWARE by design (if a series carries a `validDate` column it takes the latest revision with `validDate<=asOf` per observation date; futures carry no revision columns, so they take the simple `date<=asOf` path — we do NOT fabricate revisable data we lack). It records a PROVENANCE stamp (commodity / asOf / date-range / nRows / contracts) to the append-only `.state.provLog`. Parameter is `asOf` NEVER `asof` (the q as-of-join keyword), and `comm` not `commodity` (so the qSQL where-clause is unambiguous - the column-vs-param self-compare trap). (2) **The Market State object** `.state.build[asOf;comm]` → a dictionary assembled LAZILY per (asOf, commodity): `asOf`, `commodity`, `effectiveDate` (the latest trading date <= asOf), `curve` (the as-of curve, the SAME shape `.data.hdb.curveAt` returns so it is consistent with what regime/ sees), `spreads` (adjacent calendar spreads from the curve), `features` (a declared empty placeholder the curve engine R10 + season/carry R14 will fill - so the contract is stable now), `universe` (the tradable contracts NOT expired as of asOf), `refs` (cost-model + roll-rule handles; the real roll map is R11), and `provenance`. The state is the CONTRACT signals/templates will consume INSTEAD of raw prices, killing a class of look-ahead and roll bugs at the type level. (3) **The no-look-ahead invariants** `.state.invariant.*` (asofRespected: no row date>asOf; universeLive: the as-of curve carries no expired contract; provenanceConsistent: the stamp matches exactly what was returned) — the same invariants the R13 evidence audit will later verify on replay runs. WHY ONE DOOR: point-in-time discipline fails when it is a rule every future module must remember; make it ONE function to audit instead — the accessor PREVENTS look-ahead by construction, and R13's evidence audit will VERIFY by reading the provenance, so R9 + R13 close a loop (the door prevents; the audit proves). ADDITIVE / byte-identical: state/ is new and wraps/reads (never edits), the existing direct readers are NOT rewired, so the existing **399 tests stay byte-identical** (gamma_scalp `totalPnl=0.6521844`, short_variance `premium=11.97994`, crude 63.27->57.68, cross-commodity momentum +0.7403373/0.75, gas carry -0.0243->+0.1773 all unchanged) + 2 new synthetic state tests (`test_asof_accessor`: across several asOf dates no row date>asOf, a planted future row is excluded, the provenance matches exactly, and the revision path on a synthetic `validDate` table takes the latest `validDate<=asOf`; `test_market_state`: `.state.build` assembles the expected dict, the universe excludes a synthetic expired contract, and two as-of dates produce states where the earlier is a strict time-prefix) = **401/0**. Registered as a new R2 `state` kind (`marketState`, passing `.contracts.verify`); carded (R5) with real failure modes (revisable data not yet handled, the direct readers not yet rebased, the provenance log advisory until R13, the universe filter trusts the HDB expiry field). New `.cfg.state` (universe/liquidity filters: `expiryStrictlyAfter`, `minVolume`); `state/` loads immediately after `data/`. Demo `apps/examples/market_state_asof.q` (real CRUDE: build the as-of Market State - curve / calendar spreads / tradable universe / provenance - and show an earlier as-of is strictly a time-prefix of a later one: 12 tradable contracts max 202206 at 2021-06-17 vs 21 contracts max 202712 later). This converts the rigorous judge from rigorous-in-principle to rigorous-in-fact, and unblocks all of R10–R16 (the curve engine builds the state, roll trades through it, the replay loop folds over it, season/carry fill its features, the first real strategy is judged on it). Nothing renamed.

v0.73 (Research OS step R8, ARCHITECTURE.md §13-R8) adds **factor decomposition (PCA of the futures curve)** — the FIRST new research shape built ON the completed spine. Not new infrastructure: a new analytical CAPABILITY + a new problem TEMPLATE, plugged into the finished R1–R7 spine (registered via R2, card-able via R5, gated through R3/R3b, regime-aware via the R4 skeptic) — the realisation of the problem-template abstraction's "PCA / stochastic control / deep hedging all plug in" promise, for the first one. ADDITIVE — composes existing functions + reads existing data; changes no existing compute path. (1) **The factor capability** (`factor/factor.q`, `.factor.*`): PCA of the curve-change panel into level/slope/curvature + residuals. q has NO built-in eigendecomposition, so the top-k PCs come from DETERMINISTIC power iteration + Hotelling deflation on the covariance matrix, with determinism PINNED — a fixed initial vector, a fixed tolerance / max-iter (`.cfg.factor`), and a SIGN CONVENTION (front loading forced positive, since eigenvectors are sign-ambiguous) — so loadings/scores are byte-identical across runs and known-answer testable. `.factor.pca[X;k;cfg]` → loadings (M×k) / scores (T×k) / residuals (T×M) / explainedVar; `.factor.curvePanel` (a constant-maturity-by-rank level panel, the same front→deferred rank ordering the regime layer uses) / `.factor.changePanel` (per-maturity price DIFFERENCES — plain, not log, so it is robust to the 2020 negative-WTI print) / `.factor.decompose` (the HDB convenience wrapper). It reads `data/` downward, loads before `templates/` (which compose it), never opens the HDB at import, and is registered as a new R2 `factor` kind (`curvePCA`, passing `.contracts.verify`). (2) **The factorRelativeValue template** (`templates/factor_relative_value.q`, `.template.factorRv.*`): fade the curve's CUMULATIVE RESIDUAL from its k-factor PCA shape (the level the curve has drifted from its factor reconstruction), composing `.factor.*` + the existing RV mean-reversion engine (`.template.rv.*`) + the fill model into a per-day NET PnL series; edge source STRUCTURAL. It carries TWO template-specific gates that CAN FAIL before the universal gates run: FACTOR STABILITY (`.template.factorRv.stability` — the |cosine| of the top loading across the window's two halves ≥ minLoadingStability; a regime break that rotates the top factor fails it) and RESIDUAL STATIONARITY (reuses `.template.rv.stationarity`'s AR(1) test on the traded deviation). Registered as a `template` kind, passing `.contracts.verify`. (3) **Carded + gated** (the complete machinery): model cards for both (`curvePCA` + `factorRelativeValue`) with REAL failure modes — factor instability across regimes, k-selection overfitting, lookback-window sensitivity, and the assumption that residual reversion is tradeable rather than microstructure — and the factor template's card carries a populated failure-mode link (`covid2020`, the regime where the curve's factor structure most violently broke) so it is gateable via `.cards.gatedRun`. ADDITIVE / byte-identical: factor is new and composes/reads (never edits), so the existing **397 tests stay byte-identical** (gamma_scalp `totalPnl=0.6521844`, short_variance `premium=11.97994`, crude 63.27->57.68, cross-commodity momentum +0.7403373/0.75, gas carry -0.0243->+0.1773 all unchanged) + 2 new synthetic factor tests (`test_factor_pca`: a known-loadings 2-factor panel is recovered sign-fixed, explained-variance sums ~1, residuals ~0, byte-identical across two runs; `test_factor_template`: the factor-stability gate passes a stable structure and fails a regime break, the residual-stationarity gate passes mean-reverting and fails a random walk, and the run composes the RV engine on the factor deviation faithfully) = **399/0**. New `.cfg.factor` (k / tol / maxIter / nMaturities / minLoadingStability / residualMaturity); `factor/` loads before `templates/`. **Do NOT tune k or the lookback to pass the gates** — k-selection and the lookback are precisely where factor PCA overfits, and the deflated-Sharpe + walk-forward + holdout cascade exist to catch exactly that (so this also doubles as a stress-test that the gates do their job on a model with genuinely tempting knobs). Demo `apps/examples/factor_relative_value.q` (real CRUDE): the curve PCA recovers textbook term-structure factors (level/slope/curvature at 0.856 / 0.138 / 0.006 explained variance), the template's factor-stability + residual-stationarity gates pass, and the full bounded workflow then honestly finds NO tradeable edge (backwardation 0.40 Sharpe → DSR 0.72 < 0.95 → research; contango/flat fail cost; anyTradeable=0, escalated to the human) — not tuned. This is the first proof the spine pays off: a sophisticated shape added purely as plug-ins, judged by the same honest gates. The template the stochastic-control + deep-hedging work will follow. Nothing renamed.

v0.72 (Research OS step R7, ARCHITECTURE.md §11.7 / §13-R7) adds the **agent workforce** — the LAST roadmap item. Six BOUNDED research-agent role prompt docs + an orchestration contract + a thin single-process executable loop that COMPOSES the already-built functions. REMINDER ON SCOPE: qFDM is a BATCH research / simulation system — there is NO live trading and NO order execution; the agents produce RESEARCH OUTPUT (an honest verdict + the case) for a HUMAN, and never trade or allocate capital. Two halves: (1) **The role/contract docs** (`agents/`, NOT loaded by `core/init.q` — read by the human / Claude Code orchestration): `researcher.md` (proposes + pre-registers the hypothesis a-priori via `.gov.register`; may NOT declare anything tradeable), `validator.md` (runs `.cards.gatedRun`→`.gov.runFull`, reports the honest verdict; the ONLY role that touches the holdout, via the one-shot gate), `skeptic.md` (the adversary: surfaces the regime risk memory, hunts overfitting/post-hoc/priced-in; informs, never auto-rejects), `logger.md` (guards the honest record: every trial logged = honest N, holdout spent ≤ once, no cherry-picking), `curator.md` (ensures a capability is carded WITH a populated failure-mode field before it can be gated; maintains R5 cards + the R4 library), `lead.md` (orchestrates the loop, enforces the merge gate, escalates every go/no-go to the human, treats capital staging as human-sign-off-only) + `agents/ORCHESTRATION.md` (the loop + the seven hard invariants). (2) **The executable loop** (`workflow/workflow.q`, `.workflow.*`): `.workflow.run[proposal]` composes the BUILT functions (researcher `.gov.register` → curator `.cards.gateReady` → validator+skeptic+logger `.cards.gatedRun`→`.gov.runFull` → lead packages) and returns a HUMAN-ESCALATION PACKET (`carded`/`gatesRun`/`anyTradeable`/`verdicts`/`riskMemory`/`decision`=`escalateToHuman/`humanSignOffRequired`=1b/`stage`=`paper/`summary`). BOUNDED BY CONSTRUCTION: the loop has NO path that allocates, trades, or marks something tradeable without ALL gates passing — `anyTradeable` is a DERIVED fact (not a decision), the packet carries NO deploy/allocate/autoTrade field and ALWAYS defers to the human. It REUSES; it reimplements nothing and adds no compute path. LAYER: `workflow/` is the top functional layer (composes `.gov`/`.cards`/`.template`/`.regime` DOWNWARD only; loads after `cards/`+registry-populate, before `apps/`); opens no HDB at import. ADDITIVE / byte-identical: no compute path changed, so the existing **395 tests stay byte-identical** (gamma_scalp `totalPnl=0.6521844`, short_variance `premium=11.97994`, crude 63.27->57.68, cross-commodity momentum +0.7403373/0.75, gas carry -0.0243->+0.1773 all unchanged) + 2 new synthetic workflow tests (`test_workflow_loop`: a carded proposal runs the full loop and returns a well-formed packet, an uncarded proposal is REFUSED at the carded step with no trial logged / holdout untouched; `test_workflow_bounded`: the loop NEVER returns `anyTradeable=1b` unless a bucket passed ALL gates AND the packet ALWAYS defers to the human with no auto-deploy/allocate field, in both the non-tradeable and tradeable cases) = **397/0**. `workflow/` registered in `core/init.q` after registry-populate, before nothing (top layer); no `.cfg` addition. Demo `apps/examples/research_loop.q` (real CRUDE): the loop proposes / gates / challenges (the skeptic surfaces "resembles 2022 energy shock; late momentum chasers round-tripped") / records / packages — momentum honestly reads `anyTradeable=0`, escalated to the human, nothing deployed. The discipline lives in the SPINE, not the agents' goodwill — the holdout is sealed, the ledger append-only, gating refuses undocumented capabilities, and nothing is tradeable without every gate — so scaling the workforce never loosens it. **Deferred (recorded):** the multi-agent orchestration (Claude Code worktrees / Agent Teams / Dynamic Workflows) parallelises this same single-process loop behind the same merge gate (full suite green + byte-identical canonicals). **The R1–R7 Research OS roadmap is complete.** Nothing renamed.

v0.71 (Research OS connective wiring, ARCHITECTURE.md §11.4/§13) wires three of the standing suggestions so the R1–R6 layers INFORM each other — cycle-free, without breaking the one-directional dependency rule. STRICTLY ADDITIVE in the sense that matters: NO existing gate verdict, NO gate pass/fail, and NO pinned canonical number moves (gamma_scalp `totalPnl=0.6521844`, short_variance `premium=11.97994`, crude 63.27->57.68, cross-commodity momentum +0.7403373/0.75, gas carry -0.0243->+0.1773 all unchanged); the additions are a new gating ENTRY, a new informational ANNOTATION field, and a more COMPLETE audit. (A) **Carded gating** (in `cards/`, the legal cards->gov downward direction): `.cards.gateReady[capName]` -> 1b iff a model card EXISTS and its failure-mode / risk-memory field is populated (`riskMemoryKey` is a real link, not `na`/empty); `.cards.gatedRun[hypoId;capName;runner;axis]` prechecks gateReady and, if NOT ready, REFUSES — returns an `undocumented` verdict (tradeable=0b) and does NOT run the gates / does NOT touch the holdout; if ready it delegates to `.gov.runFull`. This realises "Gate 0 requires a card with a populated failure-mode field" at the BOUNDARY — `gov/` is NOT modified to know about cards (cards is ABOVE gov in R5; gov importing cards would cycle). (B) **The regime risk-memory skeptic** (in `gov/`, reaching DOWNWARD to `regime/` — a legal read, `regime/` is LOW): `.gov.run` / `.gov.runFull` attach an INFORMATIONAL `riskMemory` annotation to each verdict via the new `.gov.__riskMemoryNote`, which queries `.regime.analogue.forDate` + the R4 regime library for the hypothesis's (commodity, latest-date) context and surfaces the nearest historical episodes' failure modes ("resembles 2022 energy shock; late momentum chasers round-tripped"). It changes NO gate pass/fail and NO existing verdict field — it only ADDS `riskMemory`; graceful "" when there is no context / no HDB / no library. (C) **A complete audit** (in `cards/`): `.cards.__registered` now walks ALL kinds in the R2 registry dynamically (`.registry.kinds[]`, not a hardcoded list) + the strategy registry, so the R6 `template` kind — and any future kind — can never be silently missed by the coverage audit. LAYER REASONING (the crux that keeps it cycle-free): no new dependency edge points upward — cards->gov (gating) and gov->regime (skeptic) are both downward; `gov/` still does NOT import `cards/` (confirmed). ADDITIVE / byte-identical: the existing **392 tests stay byte-identical** (the gov tests exercise `.gov.evaluate`/`.gov.holdoutGate` directly, so the added `.gov.run`/`runFull` annotation field doesn't touch them; every pinned canonical unchanged) + 3 new synthetic tests (`test_gov_skeptic_annotation`: the verdict gains `riskMemory` AND every pre-existing field is byte-identical to a direct `.gov.evaluate` — the skeptic added nothing to the cascade; `test_cards_gated_run`: gateReady 1b/0b, gatedRun REFUSES an undocumented capability without logging a trial and DELEGATES to `.gov.runFull` when ready; `test_cards_audit_dynamic`: the audit covers the `template` kind and a freshly-registered synthetic kind) = **395/0**. New `.cfg.gov.skepticN`; no new layer, so `core/init.q` changes the version only (no load-order change). Demo `apps/examples/carded_gated_research.q` (real CRUDE): an uncarded capability is REFUSED (gates not run); the carded momentum signal is gated, its verdicts UNCHANGED (backwardation reject / contango / flat research) but now carrying the regime risk-memory annotation; and `.cards.audit[]` now covers the `template` kind. **Deferred (recorded):** splitting the card store into low DATA vs high LOGIC so a literal Gate 0 could live INSIDE gov (the carded ENTRY gives the same guarantee at the boundary for now). The "cohere before R7" move — R7's agents will instantiate exactly this loop (propose -> must be carded -> gated -> the skeptic surfaces the risk memory -> logged). Nothing renamed.

v0.70 (Research OS step R6, ARCHITECTURE.md three plug-in kinds / §13-R6) adds the **problem-TEMPLATE abstraction** — the THIRD plug-in kind. The architecture's three kinds are CAPABILITY (R2 contracts), KNOWLEDGE (R5 cards), and PROBLEM-TEMPLATE: a research SHAPE that composes capabilities, declares its own inputs/outputs/validation, and produces a per-day PnL series that flows into the universal gov gates. ADDITIVE — the existing directional strategies and the backtester stay BYTE-IDENTICAL (directional is expressed as a template by a FAITHFUL WRAPPER delegating to the unchanged functions; the one new shape is new code composing existing capabilities). New `templates/` layer. (1) **The abstraction** (`templates/template.q`, `.template.*`): a `template` kind on R2's `.registry.*` with a contract enforcing the uniform OUTPUT `pnl`validation`meta (inputs are template-specific, declared in each manifest but not contract-required); `.template.register`/`.get`/`.list`/`.conforms`/`.run`. A deficient template manifest (missing `pnl`/`validation`/`meta` in `out`) is REJECTED by `.contracts.verify` — the contract bites for templates too. (2) **Template #1 `directionalSignal`** — a FAITHFUL WRAPPER that DELEGATES to the unchanged `.strategy.path.commoditySignals` + `.strategy.runAndSummarize`, so its per-day PnL is BYTE-IDENTICAL by construction; the existing direct call paths are untouched (the template is an additional way to express directional, not a replacement). (3) **Template #2 `relativeValue`** (`templates/relative_value.q`, `.template.rv.*`) — a genuinely different shape: a crude CALENDAR-spread mean-reversion (front vs a deferred tenor, straight from the curve the regime layer measures), composing a new mean-reversion signal (fade extremes — the opposite of momentum, via a causal rolling z-score) and the existing fill model (`.exec.fill`, refPrice=1/notional units) into a per-day NET PnL series; edge source STRUCTURAL. It carries its OWN TEMPLATE-SPECIFIC validation that CAN FAIL: a spread-stationarity gate (`.template.rv.stationarity` — an AR(1)/OU mean-reversion test: regress spread_t on spread_{t-1}, mean-reverting iff `b<maxAr1`, OU half-life `-log2/log b`); a non-stationary (random-walk, `b≈1`) spread FAILS this gate and the RV thesis is rejected BEFORE the universal gates even run — a gate the directional template does not have (the same honesty discipline as the gov gates / R2 conformance / R5 audit). LAYER: `templates/` composes signals/backtest/execution, is INDEPENDENT of `gov/` (no mutual import — a template PRODUCES a PnL series, the gov gates CONSUME one); loads after `backtest/`+`portfolio/`, before `gov/`; never opens the HDB at import. ADDITIVE / byte-identical: the existing **389 tests stay byte-identical** (gamma_scalp `totalPnl=0.6521844`, short_variance `premium=11.97994`, crude 63.27->57.68, cross-commodity momentum +0.7403373/0.75, gas carry -0.0243->+0.1773 all unchanged — the directional wrapper delegates, proven by the faithful-wrapper test; relativeValue is new code producing new non-canonical numbers) + 3 new synthetic template tests (`test_template_registry` round-trip + a deficient manifest rejected; `test_directional_template_faithful` the directionalSignal template's per-day PnL EQUALS the existing backtest's exactly, for two configs; `test_relative_value` a known-answer fade-the-spike PnL + the stationarity gate passing a mean-reverting series and FAILING a random walk + an end-to-end run on a synthetic curve) = **392/0**. New `.cfg.templates.rv`; `templates/` loads after `portfolio/`. Demo `apps/examples/relative_value_template.q` (real CRUDE): the calendar spread PASSES its stationarity gate (AR(1)=0.52, half-life ~1 day) → flows through `.gov.runFull` (the SAME cost/deflation/walk-forward/holdout cascade the directional shapes face), which honestly finds no tradeable edge (backwardation fails cost, contango fails deflation DSR 0.80, flat fails cost; all `tradeable=0b`) — NOT tuned to pass. BOUNDED: the abstraction + contract/registry + directional-as-faithful-wrapper + ONE new template + a demo; deep-hedging/control is a named deferral. The precondition for vol / optimal-execution / deep-hedging templates later, and the shape R7's agents will instantiate. Nothing renamed.

v0.69 (Research OS step R5, ARCHITECTURE.md knowledge plug-ins / §13-R5) adds **model cards** — the KNOWLEDGE plug-in that ties the spine together. R2 gave each capability a contract; R3/R3b judge a strategy's evidence; R4 holds the regime risk memory; R5 adds a structured, queryable MODEL CARD per capability that SYNTHESISES all of it. PURELY ADDITIVE — a new HIGH layer `cards/cards.q` (`.cards.*`) that READS the R2 registries, the R4 regime library, and the gov ledger to assemble documentation; it changes no compute path. (1) **The schema + store** — `modelCards` (curated in `.cfg.cards`, persisted by `scripts/build_model_cards.q` to the splay via `.Q.en`+`set`/`get`, no `\l`): cardId, capabilityKind, capabilityName (matches the registry name), version, intendedUse, assumptions, edgeSource (`riskPremium`/`structural`/`informational` for signals/strategies, `na` for pricers/fill models), regimeApplicability, riskMemoryKey (→ the regime library), govHypoId (→ the gov record), owner, asOf; the narrative per card is in `docs/MODEL_CARDS.md`. `.cards.get`/`.list`/`.forCapability` (parameters are `capName`/`capKind`, NOT the column names — a qSQL `where capabilityName=capabilityName` would compare the column to itself). (2) **Validation status DERIVED from governance** — `.cards.validationStatus[capName]` reads the card's govHypoId, then from the gov ledger: the trial count N, the headline net Sharpe, the RECOMPUTED deflated Sharpe over the family (reusing `.gov.deflatedSharpe`), and the holdout outcome from the hypotheses registry → status `ungated` (nothing logged) / `inResearch` (logged, no holdout look) / `validated` (holdout passed) / `rejected` (holdout failed) + a `tradeable` bool (true IFF the sealed holdout passed). It can NEVER read "validated" when the ledger says the thing failed — the card's validation line reflects the actual governance record, not an assertion. `.cards.riskMemory[capName]` returns the linked regime-library failure modes (reuses R4). (3) **The audit that CAN FAIL** — `.cards.audit[]` (pure core `.cards.__auditAgainst`) walks every registered capability (the four R2 kinds + the 24 strategies) and FLAGS an undocumented registered capability, a card missing a required section (intendedUse/assumptions/regimeApplicability empty), a signal/strategy card with no named edge source ("name which of the three or it's noise"), and an orphan card (capabilityName not registered) — the same honesty discipline as the gov gates and the R2 conformance check; a check that can't say no is theatre. LAYER: `cards/` is HIGH (reads registries + gov + regime library; imported by NOTHING below it — a card sits above what it documents), loads after `gov/`, before `apps/`; never opens the HDB at import (`.cards.open` uses `get`). ADDITIVE / byte-identical: no compute path in any layer changed, so the existing **386 tests stay byte-identical** (gamma_scalp `totalPnl=0.6521844`, short_variance `premium=11.97994`, crude 63.27->57.68, cross-commodity momentum +0.7403373/0.75, gas carry -0.0243->+0.1773 all unchanged) + 3 new synthetic card tests (`test_cards_core` round-trip; `test_cards_validation_status` a logged deflation-failing no-holdout hypothesis → inResearch/not-tradeable/deflationPass=0b, a passed holdout → validated/tradeable, no link → ungated; `test_cards_audit` the audit bites on an undocumented capability + a missing section + a strategy with edgeSource `na` + an orphan card while passing the well-formed ones) = **389/0**. Curated cards for the 4 R2-registered capabilities (blackScholesFdm / commoditySignalPath / schwartz2Curve / dailyFillCost) + 3 key strategies (gammaScalp / shortVariance / timeSeriesMomentum); the audit honestly reports the remaining 21 strategies as a coverage gap (curate, don't auto-spam one per function). New `.cfg.cards`; `cards/cards.q` loads after `gov/gov.q`. Demo `apps/examples/model_card_show.q` (real CRUDE): one screen shows the momentum signal's card — identity + the R2 contract it satisfies (conforms) + assumptions + named edge + LIVE validation status derived from a just-logged `.gov.run` (inResearch, headline DSR 0.43, not tradeable — honest) + linked risk memory ("late momentum chasers round-tripped") + the audit coverage. The knowledge plug-in that makes the system legible; R6/R7 read from it. Nothing renamed.

v0.68 (Research OS step R2, ARCHITECTURE.md integration principle / §13-R2) adds the **plug-in spine: contracts & registries**. The whole architecture rests on one small generic spine + plug-ins through fixed CONTRACTS; R2 makes that concrete and bounded. PURELY ADDITIVE — it adds a registry mechanism and registers EXISTING capabilities as metadata; it does NOT change how any capability COMPUTES, and the existing direct call paths (how the backtest finds a strategy, how the pricer is invoked) are untouched. New `core/registry.q` (loads right after `config/`, before `models/` — depends on nothing above base q): (1) a GENERIC registry `.registry.*` — `new[kind;contract]` / `register[kind;name;fn;manifest]` (fn = a HANDLE to the unchanged function; manifest = its declared I/O schema; unknown kind → signal, duplicate name → overwrite-with-warn) / `get` / `list` / `unregister` / `dropKind`; (2) four PER-KIND registries built on it, each carrying a versioned CONTRACT — `.model` (pricer: in spot/strike/expiry/rate/vol → out unitPrice/notionalPrice), `.signal` (in curveHistory/sigCfg → out path), `.calibrator` (in marketCurve/modelType/calCfg → out calibratedParams/fitRmse/status), `.execution.fillModel` (in order/ctx/cfg → out fillPrice/totalCost) — each with `register`/`get`/`list`/`conforms`; (3) a conformance check that CAN FAIL — `.registry.conforms` / `.contracts.verify[]` checks each registered MANIFEST against its contract (contract version + every required in/out key present with a matching type tag; a manifest superset is fine) and REJECTS a deficient/type-mismatched/wrong-version manifest (the same discipline as the gov gates: a check that can't say no is theatre). `core/registry_populate.q` (loaded last, after every capability layer exists) registers the four existing capabilities through their registries with accurate manifests pointing at the UNCHANGED functions — `blackScholesFdm`→`.engine.priceOption`, `commoditySignalPath`→`.strategy.path.commoditySignals`, `schwartz2Curve`→`.commodity.calibrateCurve`, `dailyFillCost`→`.exec.fill` — all of which PASS `.contracts.verify[]`. Strategies keep their own existing `.strategy.register`/`.strategy.registry` (deliberately NOT rebased, preserving byte-identity). ADDITIVE / byte-identical: no compute path in any layer changed (registration is metadata only), so the existing **383 tests stay byte-identical** (gamma_scalp `totalPnl=0.6521844`, short_variance `premium=11.97994`, crude 63.27->57.68, cross-commodity momentum +0.7403373/0.75, gas carry -0.0243->+0.1773 all unchanged) + 3 new synthetic registry tests (`test_registry_core` round-trip + duplicate/unknown behaviour; `test_contracts_conform` a conforming manifest passes while a missing-output-key / missing-input-key / type-mismatch / wrong-version manifest is REJECTED, and `.contracts.verify[]` flags exactly the bad ones — proving the check bites; `test_existing_registered` the 4 real capabilities conform and their fn handles point at the real unchanged functions) = **386/0**. New `core/registry.q` + `core/registry_populate.q`; `docs/CONTRACTS.md` (the contracts, documented); demo `apps/examples/registry_inventory.q` (the inventory + conformance, one screen). BOUNDED: the mechanism + the contracts + registering what exists + a conformance test — NOT a migration of every call site and NOT a new capability; the registry is the canonical lookup for NEW work + introspection/governance, and the foundation R5 (model cards) / R6 (problem-templates) / R7 (agents) plug into. ARCHITECTURE.md header + the regime layer-table row brought current (had drifted). Nothing renamed.

v0.67 (Research OS step R4, ARCHITECTURE.md Part II §11.3 / §13-R4) adds the **regime/analogue library + risk memory** — the "digested history" side of the research OS. R1 MEASURES the current regime, R3/R3b JUDGE a strategy's evidence; R4 lets the workflow ask "this state resembles WHAT?" and "what killed strategies in that state?" from explicit, queryable records. New `regime/analogue.q` (`.regime.analogue.*` + `.regime.library.*`). PURELY ADDITIVE — it extends `regime/` and touches no engine/backtest/gov `.q` code path. Three parts: (1) **The episode library** — a splayed `regimeEpisodes` table (built by `scripts/build_regime_library.q` from the `regimes` table, mirroring the HDB build: `.Q.en` + `set`, via `get`/`set` not `\l`) of NAMED historical crude episodes, each with a COMPUTED dominant fingerprint (`.regime.library.__fingerprint`: modal curveState/volState/liqState/rollPhase/seasonPhase + mean slopePct/volPct/volumePct over the episode's date range), its drivers, and a risk-memory summary; the curated spec is `.cfg.regime.episodes` and the full failure-mode writeups + out-of-data narrative live in `docs/REGIME_LIBRARY.md`. (2) **The analogue engine** (pure, testable) — `.regime.analogue.distance[a;b]` is a weighted Euclidean over the three percentile axes (each in [0,1]) PLUS a per-mismatch categorical penalty over the five discrete states (weights in `.cfg.regime.analogue`); identical fingerprints → 0. `.regime.analogue.nearest[state;n]` / `.regime.analogue.rank[state;episodes;n]` rank the episodes by distance and return the n nearest with their label + driversKey + risk-memory summary; `.regime.analogue.forDate[commodity;date;n]` computes a (commodity,date)'s own fingerprint (reusing `.regime.label`) then `.nearest`. (3) **The deliverable** — `apps/examples/regime_analogue_today.q` takes the latest crude date's fingerprint and prints the nearest historical episodes + their risk memory (real CRUDE: today's backwardation + high-vol state most resembles `crude_2022_energyShock`, distance 1.05, NOT the 2020 contango/storage crash at 2.23 — the engine distinguishes the regimes correctly). HONEST DATA SCOPE: episodes are matched ONLY on in-HDB-coverage windows (crude ≈ 2018.12 → 2026), each with a real computed fingerprint; out-of-data lessons (2008 GFC, 2014-16 shale glut) are NARRATIVE ONLY in `REGIME_LIBRARY.md`, never fabricated as fingerprints. LAYER RULE: `regime/` stays a LOW layer — `analogue.q` reads the `regimes` + `regimeEpisodes` tables and reuses `.regime.label`, and does NOT import `gov/` or `backtest/` (gov ABOVE regime may consult the analogue — a legal downward call — never the reverse); never opens the HDB at import (`.regime.library.open` uses `get`). ADDITIVE / byte-identical: the engine/backtest/gov code is untouched, so the existing **381 tests stay byte-identical** (gamma_scalp `totalPnl=0.6521844`, short_variance `premium=11.97994`, crude 63.27->57.68, cross-commodity momentum +0.7403373/0.75, gas carry -0.0243->+0.1773 all unchanged) + 2 new synthetic regime tests (`test_regime_analogue_distance` known-answer: identical→0, a 0.4 pct gap→0.4, a categorical mismatch→+catPenalty, combinations add; `test_regime_analogue_nearest` ranks the closest episode first / a distant one last on a synthetic episode set + the schema round-trips) = **383/0**. New `.cfg.regime` keys `analogue` (distance weights) + `episodes` (the curated spec); `regime/analogue.q` loads after `regime/regime.q`. (Optional later: a gov-side risk-memory check consulting `.regime.analogue` in the thesis/skeptic gate — gov above regime, a legal downward call.) Nothing renamed.

v0.66 (Research OS step R3b, ARCHITECTURE.md Part II §11.4 / §13) **completes the gate cascade** with the sealed-holdout zone + the one-shot holdout gate, and closes the R3 disposition hole with fail-safe verdicts. PURELY ADDITIVE — it extends `gov/gov.q` with gov-side DATE-RANGE slicing only; no engine/backtest/regime/data `.q` code path changes (the existing HDB/backtest queries are untouched). Three parts: (1) **Three data zones** — `.gov.zone.boundaries[dates]` (pure, floor-based cut indices) / `.gov.zone.range[commodity;zone]` split each commodity's sorted dates into `train`/`validate`/`holdout`/`trainValidate` (`.cfg.gov.zones` = 0.6/0.2, holdout the most-recent 0.2 — out-of-sample in TIME); gates 0-3 and ALL exploration operate on `trainValidate` ONLY. (2) **The sealed holdout (Gate 4, ONE-SHOT)** — `.gov.holdout.read[commodity]` is the SOLE access path to the holdout range (an API-level seal, called only by the gate). `.gov.holdoutGate[hypoId;runner]` runs the strategy over the holdout via `runner[from;to]`, scores NET Sharpe + DSR, checks the `.cfg.gov.holdoutSharpe` bar, and is ONE-SHOT: it records the look immutably (a `holdoutUsedAt` timestamp + result columns on `hypotheses`, a ledger row with dataZone=`holdout`); a SECOND call for the same hypothesis returns the RECORDED verdict WITHOUT recomputing or re-reading the holdout (one look per hypothesis, ever). `.gov.runFull[hypoId;runner;axis]` is the complete 5-gate orchestrator — restrict to `trainValidate`, run gates 0-3 per regime bucket, and ONLY if a bucket cleared 0-3 does the hypothesis earn the one-shot holdout look (the seal protects holdout from unearned looks); the `runner[from;to]` callback is the seam that lets gov control which dates the strategy EVER sees (R3's `.gov.run`/`.gov.evaluate` are kept intact for the no-holdout path). (3) **Fail-safe verdicts** — a gate FAILURE can only produce `reject` (Gate 0/1: no mechanism / no edge after cost) or `research` (Gate 2/3/4: plausible thesis, failed evidence), NEVER tradeable; the only tradeable verdicts are `pass` and `regimeConditional` (cleared every gate but the edge is regime-gated), which REQUIRE passing ALL gates incl. holdout. A **`tradeable` boolean** (true IFF every gate passed) is added to every verdict record — downstream must read it, never infer from the label. This FIXES the R3 hole where a deflation-FAILED slice wore the promotable-looking `regimeConditional` label: contango/flat now correctly read `research`. ADDITIVE / byte-identical: the engine/backtest/regime code is untouched, so the existing **378 tests stay byte-identical** (gamma_scalp `totalPnl=0.6521844`, short_variance `premium=11.97994`, crude 63.27->57.68, cross-commodity momentum +0.7403373/0.75, gas carry -0.0243->+0.1773 all unchanged) + 3 new synthetic gov tests (`test_gov_zones` zone boundaries / trainValidate excludes holdout / contiguity; `test_gov_holdout_oneshot` first look records + second look returns the recorded verdict without recomputing via a runner counter, and the runner never sees a trainValidate date; `test_gov_verdict_failsafe` failures are non-tradeable with reject/research labels, all-pass is tradeable with pass/regimeConditional) — and `test_gov_gates` updated for the corrected mapping (post-hoc deflation FAIL → `research`; + `tradeable` asserts; + an all-pass-post-hoc → `regimeConditional` case) = **381/0**. New `.cfg.gov` keys `zones` + `holdoutSharpe`; the `hypotheses` table gains `holdoutUsedAt`/`holdoutVerdict`/`holdoutNetSharpe`/`holdoutDsr` columns. `.claude/agents/test-runner.md` corrected: the FULL suite runs from the MAIN THREAD (a subagent's backgrounded job dies on turn-end), the subagent does GROUP runs. Demo `apps/examples/gov_gate_momentum.q` re-run through `.gov.runFull` (real CRUDE): zones print, gates 0-3 run on trainValidate, backwardation FAILS cost → `reject`, contango (DSR 0.30) / flat (DSR 0.51) FAIL deflation → `research`, every bucket `tradeable=0b`, and the sealed holdout is NEVER read (no bucket earned the look — the seal held). The cascade is now whole and fail-safe. **Deferred (recorded, not built):** the "what's PRICED IN" gate → later (needs options-surface/positioning data the HDB lacks — no faked stub). Nothing renamed.

v0.65 (Research OS step R3, ARCHITECTURE.md Part II §11.4 / §13-R3) adds the **research-governance layer** — the anti-overfitting heart. R1 (`regime/`) SURFACES regime-conditional structure; R3 (`gov/gov.q`, `.gov.*`) JUDGES it and refuses to be fooled by a tempting small-sample slice. Four pieces: (1) a hypothesis **registry** (`.gov.register` / `.gov.hypo`, pre-registration with thesis / edgeSource / claimedRegimes); (2) an **APPEND-ONLY trials ledger** (`.gov.logTrial` / `.gov.trials` / `.gov.nTrials` — the honest `N`, the multiple-testing denominator; a second log ADDS a row, never overwrites); (3) the **deflated-Sharpe** statistical core (Bailey & López de Prado): `.gov.psr` (Probabilistic Sharpe Ratio), `.gov.deflatedSharpe` (PSR vs the expected MAX Sharpe under the null from N trials, with `V` = variance of the family's per-period Sharpes), and `.gov.phiInv` (inverse normal via Acklam — added because no inverse-normal existed; `Phi` REUSES the BS-core `.validation.__normalCdf`). The CONSISTENCY TRAP is handled: `__perf` reports an ANNUALISED Sharpe, the DSR needs the PER-PERIOD one, so the layer converts `annualised/sqrt(annDays)`. (4) An ordered **gate cascade** (`.gov.evaluate`, stops at first failure): Gate 0 thesis (+ a POST-HOC flag when the tested bucket is not in claimedRegimes), Gate 1 cost (net-of-execution Sharpe ≥ hurdle, after a REALISTIC `.exec` cost config from `.cfg.gov`), Gate 2 deflated Sharpe (DSR ≥ 0.95, N from the ledger — where small-sample slices die), Gate 3 walk-forward (OOS sign-stability, REUSING `.strategy.commodityBT.__splits` causally as `.alloc.compare` does). Verdicts: `pass`/`reject`/`research`/`regimeConditional`. The non-optional path `.gov.run[hypoId;pnlByDate;regimeLabels;axis]` ALWAYS logs a trial per regime bucket THEN evaluates — so logging cannot be silently skipped and the backtest ENGINE is never edited (that is how logging stays non-optional WITHOUT touching engine code). ADDITIVE / byte-identical: `gov/` is a HIGH layer (it MAY import `backtest/`/`regime/` — downward is legal), loads after `backtest/`+`portfolio/` / before `apps/`, never opens the HDB at import (`.gov.open` uses `get`, not `\l`; `.gov.flush` persists). The engine/backtest/regime code is untouched, so the existing **375 tests stay byte-identical** (gamma_scalp `totalPnl=0.6521844`, short_variance `premium=11.97994`, crude 63.27->57.68, cross-commodity momentum +0.7403373/0.75, gas carry -0.0243->+0.1773 all unchanged) + 3 new synthetic gov tests (`test_gov_deflated_sharpe` known-answer PSR(0)=0.5 / phiInv(0.975)≈1.96 + monotonicity in n/N/skew; `test_gov_ledger` append-only N grows; `test_gov_gates` the four gate verdicts + stop-at-first-failure + the post-hoc flag) = **378/0**. New `.cfg.gov` (hurdle / DSR threshold / valid edgeSources / realistic exec cost / walk-forward split scheme); new `gov` test group. Demo `apps/examples/gov_gate_momentum.q` (real CRUDE): R1's regime-conditional momentum run NET of a realistic execution cost, gated — the honest verdict is exactly the predicted one: backwardation FAILS Gate 1 (cost; net Sharpe goes negative under realistic slippage), and the tempting contango (DSR 0.35, 249 obs) / flat (DSR 0.43, 157 obs) slices FAIL Gate 2 (deflated Sharpe) and are flagged POST-HOC → `regimeConditional`. R1 surfaced the +2.56/+0.89 structure; R3 sinks it on cost + deflation + the post-hoc penalty. **Deferred (recorded, not built):** the sealed-holdout zone + one-shot holdout gate → R3b (data-zone segregation deserves its own step; walk-forward provides OOS rigor for now); the "what's PRICED IN" gate → later (needs options-surface/positioning data the HDB lacks — no faked stub). Unblocks R3b and every problem template that rides the same gates. Nothing renamed.

v0.64 (Research OS step R1, ARCHITECTURE.md Part II §11.1-11.2 / §13-R1) adds the **regime layer** — the seam between the compute engine and the research operating system. New `regime/regime.q` (`.regime.*`) deterministically MEASURES a per-(commodity,date) regime fingerprint from the existing HDB `futures` data (no prediction, no opinion): `curveState` (front-vs-deferred relative slope -> backwardation/contango/flat), `volState` (annualised realized vol of front-settle log-returns, roll-day returns zeroed -> low/normal/high), `liqState` (front volume -> thin/normal/deep), `rollPhase` (front days-to-expiry -> near/mid), `seasonPhase` (peak/trough/neutral, REUSING `signals/seasonality`), each with a causal trailing-window percentile (`slopePct`/`volPct`/`volumePct`, a floor-free fraction). API: `.regime.series[commodity;dates]`, `.regime.label[commodity;date]` (causal — history up to the date), `.regime.breakdown[pnlByDate;regimeLabels;axis]` (groups a backtest's daily PnL by regime bucket, REUSING `.strategy.commodityBT.__perf`, with a `(blended)` row that reproduces the backtest headline exactly). Pure core (`__panelFromLong`/`__labelPanel`) is HDB-free and known-answer tested. Thresholds in `.cfg.regime`. The splayed `regimes` table (commodity `p#`/date/5 label syms/3 pct floats) is built alongside `futures` by `scripts/build_regimes.q` (`.regime.buildTable`, mirrors `ingest_hdb.q`: `.Q.en` + `set`); `.regime.open` uses `get` (not `\l`) so it never changes the working directory. ADDITIVE / byte-identical: `regime/` depends only on `data/`+`signals/`, never imports `backtest/` (the `__perf` call is lazy, a composition), loads after `signals` / before `execution`, and never opens the HDB at import — the engine/backtest are untouched, so the existing **373 tests stay byte-identical** (gamma_scalp `totalPnl=0.6521844`, short_variance `premium=11.97994`, crude 63.27->57.68, momentum +0.7403373/0.75, gas carry -0.0243->+0.1773 all unchanged) + 2 new synthetic regime tests (`test_regime_label` known-answer curve/vol/liquidity states + percentiles; `test_regime_breakdown` per-bucket `__perf` == hand split + `(blended)` == all-data headline) = **375/0**. Demo: `apps/examples/regime_conditional_backtest.q` (an existing strategy's blended Sharpe broken down by curve state — the honest "structural edge or one lucky regime?" view). This is the seam the rest of the research OS hangs off; it unblocks R3 (regime-conditional gates), R4 (analogue engine), and the regime library. New `.cfg.regime`; new `regime` test group; nothing renamed.

v0.63 (architecture migration step 4c, ARCHITECTURE.md §5/§9) closes the v0.61/4b remainder so EVERY equity trade — hedge AND discrete option legs — routes through `.exec.fill`; Step 4 is now FULLY complete on both desks. Part 1: the per-run `exec` sub-config is threaded into the hedge inputs (init + step, via `.strategy.__withExec`) of ALL hedged strategies (was gammaScalp + shortVariance only), enabling hedge-slippage realism everywhere; absent exec -> frictionless -> byte-identical. Part 2 (the substantive half): the discrete OPTION-LEG costs flagged in 4b — `legEntryTxnCost`, `rollTxnClose/OpenCost`, `bookEntryTxnCost`, `vegaEntryTxnCost`/`vegaTradeTxn`, `legTxnCost`, the spread-option `optTxn`/`hedgeTxn`, the commodityCalendar futures roll, and the dispersion/barrierHedge/powerSpike/spark/crack inline costs — all now route through one shared helper `.strategy.__legCost[notional;stratCfg]` (21 call sites). The legacy forms are uniformly `rate*notional` with notional>=0 (units stored positive, prices abs'd), so passing the dollar notional with refPrice=1 makes `.exec.fill`'s proportional cost reproduce the legacy per-leg cost EXACTLY (default frictionless) while slippage = `|notional|*slippageBps/1e4` is the premium-scaled option bid-ask (the dominant execution cost for option structures). Because `__legCost` takes `stratCfg` directly, option-leg slippage is available for ALL strategies with no per-strategy threading. Cost threads into both stepPnl and cash, so the deltaPV==stepPnl identity is preserved. Suite **373/0** — all prior tests byte-identical (gamma_scalp `totalPnl=0.6521844`, short_variance `premium=11.97994`; ALL 17 equity accounting-identity residuals unchanged to the digit, e.g. calendarRoll 4.742734e-015, iron_condor 2.107689e-016) + 1 new test (`test_exec_equity_legcost`: per-form legacy-equivalence default==frictionless, premium-scaled slippage linear in bps, gross-net==slippage paid, identity holds under option-leg slippage). Commodity headlines unchanged (crude 63.27->57.68, momentum +0.7403373/0.75, gas carry -0.0243->+0.1773). `apps/examples/execution_gross_vs_net_equity.q` extended with an iron-condor option-leg-slippage table alongside the gamma-scalp hedge-slippage table. `.exec.fill`, the commodity BT, the portfolio allocator, and all engine/driver/registry/pricer signatures unchanged; nothing renamed. ARCHITECTURE.md §9 records that steps 6 (IPC services) and 7 (CI + scheduled pipeline) are DEFERRED until production onboarding / adequate hardware.

v0.62 (architecture migration step 5, ARCHITECTURE.md §6/§9) adds the **portfolio allocator** (`portfolio/portfolio.q`, `.alloc.*`) — allocate across strategies from their net-of-execution return panels (N×T), evaluate the combined portfolio OUT-OF-SAMPLE via causal walk-forward, and COMPARE methods. `.alloc.weights[returns;method;cfg]` implements `equalWeight` (1/N baseline), `inverseVol`, `minVariance` (Σ⁻¹1), `riskParity` (ERC via cyclical coordinate descent — **the DEFAULT**, uses only the covariance so it doesn't overfit the noisy expected returns), `maxSharpe` (Σ⁻¹μ), `meanVariance` ((1/λ)Σ⁻¹μ). Constraints (`.cfg.alloc`): `longOnly`+`fullyInvested` via Euclidean capped-simplex bisection projection, `weightCap`, `turnoverPenalty` (proximal shrink toward previous weights — the tractable rebalancing-cost proxy), optional covariance `shrinkage` toward the diagonal. `.alloc.backtest`/`.alloc.compare` estimate weights on each split's TRAIN columns only and apply them to the OOS test window (CAUSAL — no future return enters a weight; reuses `.strategy.commodityBT.__splits` for index-based splits and `__perf` for scoring), ranking methods by OOS Sharpe with equalWeight as the baseline (the honest "does optimization beat 1/N OOS?" deliverable). The namespace `.alloc.*` is deliberately distinct from analytics' option-book `.portfolio.*` and the v0.45 cross-path ensemble dashboard `.strategy.portfolio.*`. ADDITIVE — consumes existing returns, touches no upstream code path, so the existing **370 tests stay byte-identical** (gamma_scalp `totalPnl=0.6521844`, short_variance `premium=11.97994` unchanged) + 2 new allocator tests (known-answer: equalWeight=1/N exact, inverseVol=1/σ normalized, minVariance/maxSharpe closed forms, meanVariance=(1/λ)Σ⁻¹μ, riskParity equal risk contributions; constraints bind: weightCap, turnover reduces churn, sum-1/long-only; causality: train-slice weights unchanged when future data is appended) = **372/0**. New `.cfg.alloc` defaults; `core/init.q` loads `portfolio/portfolio.q` after the backtest layer. Demo: `apps/examples/portfolio_allocation.q` (real commodity strategies' net returns via the HDB). No existing signature or output changes; nothing renamed.

v0.61 (architecture migration step 4b, ARCHITECTURE.md §5/§9) routes the EQUITY strategy engine through the execution layer (`.exec.fill`), completing Step 4 (the commodity BT was wired in v0.60). The high-leverage interposition point is the SHARED delta-hedge helper `.strategy.__hedgeStep` (+ `__hedgeInit`): wiring it once covers the underlying-rebalancing turnover for every hedged strategy. The equity hedge cost is price-scaled (`|hedgeTrade|*spot*rate`), unlike the commodity BT's price-free cost, so the hedge order is passed as the dollar NOTIONAL (`hedgeTrade*spot`) with refPrice=1 — then the generic `.exec.fill` proportional cost (`rate*|notional|`) reproduces the legacy cost exactly and slippage = `|notional|*bps`. A `fillFraction` (== 1f exactly under no cap) keeps the default byte-identical and lets a participation cap fill partially so the held hedge lags target (residual exposure); cost threads into BOTH stepPnl and cash so the `deltaPV==stepPnl` accounting identity is preserved. A per-run `exec` sub-config is threaded into the hedge inputs via the new `.strategy.__withExec` helper (wired for gammaScalp + shortVariance; absent → frictionless → byte-identical for all 13 hedged strategies). Suite **370/0** — the existing **368 unchanged byte-identical** (gamma_scalp `totalPnl=0.6521844`, short_variance `premium=11.97994`, `initCashAdj=49.95`, all 17 equity accounting-identity residuals within tolerance ~1e-14..1e-16) + 2 new equity execution tests (legacy-equivalence: default exec == explicit frictionless, hedge cost == legacy `|hedgeTrade|*spot*rate`; realism: deltaPV==stepPnl holds under slippage, gross−net == slippage paid, net < gross, cap lags target). Commodity headlines unchanged (crude 63.27->57.68, momentum +0.7403373/0.75, gas carry -0.0243->+0.1773). `apps/examples/execution_gross_vs_net_equity.q` (synthetic gamma-scalp) shows the honest hedge-slippage decay: gross totalPnl ~0.69 (Sharpe-like ~2.6) -> ~0.25 @20bps -> negative by ~50bps. **Step 4c remainder (flagged, not done):** per-strategy OPTION-LEG costs (`legEntryTxnCost`/`rollTxnClose|OpenCost`/`bookEntryTxnCost`/`vegaTradeTxn`/`legTxnCost`) remain the legacy flat cost — not yet routed through `.exec.fill`; threading `exec` into the other 11 hedged strategies is a one-line `__withExec` wrap each. `.exec.fill` itself, the commodity BT, and all pricing/engine/driver/registry signatures unchanged; nothing renamed.

v0.60 (architecture migration step 4, ARCHITECTURE.md §5/§9) adds the **execution-simulation layer** - a generic daily fill-and-cost model that turns target positions into simulated fills + explicit costs, so the backtest books net-of-execution PnL and reports GROSS vs NET. New `execution/execution.q` (`.exec.*`): `.exec.fill[order;ctx;cfg]` (pure) returns filledQty/fillPrice/proportionalCost/slippageCost/fixedCost/totalCost - full fill unless a `participationCap` binds (`filled=min(|order|,cap*barVolume)`), proportional cost `rate*|filled|` (the legacy rate, no price), slippage `|filled|*refPrice*bps/1e4` (adverse), optional fixed + size/ADV impact + vol-scaling; `.cfg.exec` config; `.exec.__resolve` injects the strategy's txnCostRate as proportionalRate. **Default config is frictionless and byte-identical to the legacy flat-cost path**: only the COMMODITY backtest is wired (`.strategy.commodityBT.coreInit`/`coreStep` route the cost+fill through `.exec.fill`, tracking the actually-filled position - which lags target under a cap - and preserving the exact one-step lag), so the suite is **368/0** (the existing 365 unchanged byte-identical: gamma_scalp `totalPnl=0.6521844`, short_variance `premium=11.97994`; + 3 new execution tests: known-answer fill/cost, legacy-equivalence round-trip, slip+cap realism). The EQUITY strategy engine is untouched (deferred to step 4b). `coreSummary` gained cost attribution (grossSharpe = Sharpe of stepPnl+cost, proportional/slippage/fixed totals, filled/target turnover, fillRatio) without altering the pinned walk-forward `aggregate`/`splits` schema (walkForward selects specific columns). Real-data headlines unchanged under the default config (crude 63.27->57.68, cross-commodity momentum mean +0.7403373 / 0.75, gas carry -0.0243 -> +0.1773). Realism is opt-in via a per-run `exec` sub-config; `config/dev.q` unchanged. `apps/examples/execution_gross_vs_net.q` (HDB, data-conditional) reports GROSS (frictionless) vs NET OOS Sharpe per strategy across slippage levels - the honest finding: because the vol-targeted positions trade frequently against price-scaled slippage, even sub-bp slippage erodes the single-history CRUDE edge sharply (gross momentum ~0.013 -> negative by ~1bp). No equity/pricing/parser/calibration signature or output changes; nothing renamed.

v0.59 (architecture migration step 3, ARCHITECTURE.md §3/§9) stands up a partitioned-free **splayed HDB** as the data store, the first step that changes the data PATH (CSV re-parse -> columnar read) - additive, not a rewrite. New `data/hdb.q` (`.data.hdb.*`) + `scripts/ingest_hdb.q`; `.cfg.paths.hdb` (default `data/hdb`, gitignored alongside the CSVs). Table `futures` is SPLAYED + UNPARTITIONED (chosen over date-partitioning: ~48k rows total, so ~1900 daily partition dirs would be slow + Windows-hostile): columns `commodity`(sym, `p#`), `contractYM`(long), `expiry`/`firstDate`/`date`(date), `open`/`high`/`low`/`settle`(float), `volume`(long), sym-enumerated via `.Q.en` and written with `set`. Ingestion **reuses `.parser.futures.loadAll` verbatim** (expiry=MAX / firstDate=MIN derived exactly as the parser), so the store faithfully reproduces the parser's data; the query layer (`.data.hdb.curveAt`/`curveHistory`/`dates`) materialises the parser-schema long table and feeds it to the existing `.parser.crude.curveAt`/`curveHistory`, making output **byte-identical to the parser by construction**. `.data.hdb.open` uses `get` (NOT `\l`) - a `\l` of a DB dir changes the process working directory and would break later relative-path reads (it did, in the test runner; fixed). The parser is UNCHANGED (now the CSV-read stage feeding ingestion); `core/init.q` loads the query layer but never opens the HDB at import (a fresh checkout / CI has no HDB). Library stays **365/0** (the existing 364 byte-identical + 1 new synthetic equivalence test `tests/realdata/test_hdb_ingest_query_equivalence.q` that builds a temp HDB under `scratch/` from synthetic Barchart CSV text and asserts HDB curveAt/curveHistory == parser; canonical numbers unchanged: gamma_scalp `totalPnl=0.6521844`, short_variance `premium=11.97994`). Real-data examples repointed to prefer the HDB (data-conditional, parser fallback) - headlines unchanged through the HDB: crude front 63.27->57.68, cross-commodity `timeSeriesMomentum` mean +0.7403373 / 0.75 of cells, gas carry raw -0.0243 -> deseasonalized +0.1773, NG seasonal winter +0.146 / summer -0.061. Measured speedup: CRUDE data-load 1884 ms (CSV parse) -> 396 ms (HDB) ~= 4.8x; full build 49,078 rows / 1,866 dates in ~0.4 s. No real CSV/HDB committed (gitignored). No pricing/parser/calibration/strategy signature or output changes.

v0.58 (architecture migration step 2b, ARCHITECTURE.md §2/§9) completes the config layer by sweeping the `backtest/` layer's remaining hardcoded defaults into `.cfg` — same value-preserving substitution as v0.57. Every per-strategy `defaultConfig` (17 equity: gammaScalp, shortVariance, calendarRoll, riskReversal, modelDisagreement, deltaVegaHedge, longVol, collarTailHedge, putRatioBackspread, ironCondor, barrierHedge, jumpPremium, dispersion, powerSpikeCapture, commodityCalendar, sparkSpread, crackSpread; 7 commodity: convenienceYieldCarry, chiReversion, timeSeriesMomentum, twoTimescale, storageCashCarry, carryMomentumCombo, curveRelativeValue) plus the commodity signal-path config `.strategy.path.__commodityDefaultSigCfg` now live as `.cfg.strategy.<name>` / `.cfg.strategy.commoditySignals` dicts in `config/base.q`, transcribed verbatim (exact tokens/types/key-order: `1f%252f` kept as the expression; longs like `rebalanceInterval=1`/`rollDaysBeforeExpiry=5`/`momentumLookback=20` stay long; `ratioN=2f`/`heatRate=8f` float; `trainEndDate=0Nd` null date; `hedgeOptionExpiryYears=0n`; `()!()` empty bundles; nested `jumpModelParams`/`kalmanEstCfg` dicts). Each `defaultConfig` body is now a one-line return of its `.cfg.strategy.*` dict; the generic dispatcher `.strategy.defaultConfig[strategyName]` is unchanged. Verified `~`-identical (value AND type) for all 24 + the sig config; suite stays **364/0 byte-identical** (gamma_scalp `totalPnl=0.6521844`, short_variance `premium=11.97994`, crude 63.27->57.68, cross-commodity momentum mean +0.74 / 0.75 of cells). `config/dev.q` gains a demo strategy override (`gammaScalp.deltaBand=0.10` under `QPRICER_ENV=dev`; default path unchanged). Left inline by design: the fixed Crank-Nicolson 40×50 grid inside `.strategy.realCurveCalendarRoll` (a driver grid, not a `defaultConfig` — treated like a test fixture). The config layer (Step 2) is now complete across all layers; no namespace/function renamed, no logic changed beyond literal->`.cfg` lookup.

v0.57 (architecture migration step 2, ARCHITECTURE.md §2/§9) stands up the `.cfg` configuration layer and sweeps the central tunable defaults out of the modules into it — a value-preserving substitution, not a retuning. `core/cfg.q` (loaded FIRST by `core/init.q`) reads `QPRICER_ENV` and loads `config/base.q` (every default = the exact prior hardcoded literal, types and key-order preserved), then optionally `config/{env}.q` to override via upsert; unset `QPRICER_ENV` loads base only, so the suite stays **364/0 byte-identical** (gamma_scalp `totalPnl=0.6521844`, short_variance `premium=11.97994`, crude 63.27->57.68 all unchanged). `config/dev.q` is a demo override (coarser FDM grid, smaller MC path count, `output/dev`), verified to take effect under `QPRICER_ENV=dev` while the default path is unchanged; an unknown env falls back to base with a warning. Externalized this step: **core** — `.cfg.fdm` (`.config.defaultPricingConfig`/`withDefaults`), `.cfg.iv` (`.config.defaultImpliedVolConfig`), `.cfg.mc` (`.montecarlo.defaultMcConfig`); **calibration** — `.cfg.calib.curve`/`curveSeries` (`.commodity.curveCal.defaultCalCfg`/`defaultSeriesCfg`), `.cfg.calib.kalmanParams`/`kalmanEst` (`.commodity.kalman.defaultParams`/`defaultEstCfg`); **analytics** — `.cfg.analytics.greek`/`disagreement`/`modelRiskLimit` (the three `.commodity.modelreport.default*Config`); plus `.cfg.paths` (default barchart/output dirs — a documented registry for apps; the library still takes dirs as explicit args, so not test-affecting). Each swept function now simply returns its `.cfg.*` dict. **Left inline (constants, not config):** algorithmic/structural literals (the `-0.5*sigma^2` drift half, variance `2`s, factor counts, grid-shape arithmetic, the `252`-day annualisation inside formulas). **Deferred to Step 2b (whole `backtest/` layer):** the 14 per-strategy equity `defaultConfig` dicts and `.strategy.path.__commodityDefaultSigCfg`; `models/`+`signals/` need no sweep (caller-supplied params). No namespace/function renamed; no logic changed beyond literal->`.cfg` lookup.

v0.56 (architecture migration step 1, ARCHITECTURE.md §9) layers the repository: the flat `lib/` is split into the layer tree (`core` / `config` / `data` / `models` / `calibration` / `analytics` / `signals` / `execution` / `backtest` / `portfolio` / `services` / `apps` / `scripts`), each with a `README.md`; the loader becomes `core/init.q`; `examples/` and the commodity demo move to `apps/`; all `\l lib/init.q` load paths become `\l core/init.q`. This is a PURE STRUCTURAL MOVE — file locations + load paths only, no logic edits and no renamed functions/namespaces. The loader preserves the exact proven load ORDER (only the paths changed), so the full suite stays 364/0 and byte-identical: canonical numbers unchanged (gamma_scalp `totalPnl=0.6521844`, short_variance `premium=11.97994`, all Kalman/calibration/strategy numbers), and the crude example still gives 63.27 -> 57.68. `.parser.crude.*` / every namespace is unchanged. Reserved layers (config/execution/portfolio/services/scripts) are empty skeletons for later migration steps (config, HDB, execution sim, portfolio optimizer, CI/pipeline). One mixed file (`commodityStrategies.q`, signal + backtest halves) was left whole in `backtest/`; its signal half splits to `signals/` in a later step.

v0.55 (cross-commodity robustness) validates the strategy suite on a SECOND commodity. `.strategy.path.commoditySignals` gains a `deseasonalize` flag (default off -> WTI byte-identical) and `productTag`: for gas, monthly seasonal factors are fit TRAIN-ONLY (causal) and divided out of the SIGNAL curve before extracting carry/Kalman/curve-residual signals, while the tradeable front-return series stays raw. `.strategy.commodityBT.crossCommodity[detailByCommodity]` aggregates walk-forward OOS across (commodity x split) into mean Sharpe + dispersion + fraction-of-cells-positive, ranked. The honest cross-commodity verdict (CL all 1866 dates + deseasonalized NG, 10 rolling 252/63 splits, 20 cells each): `timeSeriesMomentum` is the robust performer (mean +0.74, positive in 75% of cells); `twoTimescale` - the v0.54 WTI-2020/2021 standout (+1.77) - COLLAPSES on the full window (-0.05 cross-commodity), exposing it as period-specific; and deseasonalization flips the gas-carry verdict (raw OOS Sharpe ~-0.02 -> deseasonalized +0.18/+0.80). docs/COMMODITY_DESK.md gains the cross-commodity subsection with the plainly-stated caveat (two commodities, ~2018-2026 dominated by 2020, wide error bars). 3 synthetic tests (deseasonalize round-trip + flattening, signal deseasonalization causality + WTI-unchanged, cross-commodity aggregation arithmetic + stable rank). WTI signal output byte-identical; no reused-module signature/output change; no real CSV committed.

v0.54 (closeout) hardens the strategy verdict and exercises the built-but-unproven pieces. Part A: `.strategy.commodityBT.walkForward[strategies;trade;curveHistory;sigCfg;splitCfg]` runs the existing per-run backtest over multiple sequential train/test splits (rolling or expanding, index-based over the distinct curve dates so appending data adds splits without changing existing ones - the cross-split causality guarantee; Kalman re-estimated per split on that split's train only), aggregating each strategy's OOS metrics into mean +/- dispersion + splits-positive. The honest finding (real WTI, 5 rolling 180/63-day splits): the single-2021-split "winner" `curveRelativeValue` (Sharpe 2.19) is NEGATIVE in all 5 splits (mean -1.76) - a fluke draw - while `twoTimescale` is the robust standout (mean +1.77, positive in 4/5); dispersions ~1.3-3.2 confirm a one-year Sharpe has a +/-1-2 standard error, so the distribution is the verdict, not a point estimate. Part B: docs/COMMODITY_DESK.md gains a "Does it trade? out-of-sample strategy verdict" section with both tables and the plainly-stated caveats (negative findings framed as the valuable ones). Part C: the parser is generalized to any product via `.parser.futures.loadAll[dir;productTag]` / `listFiles` (Day_<TAG>_YYYYMM00), reusing crude's tag-independent CSV/curve helpers verbatim so `.parser.crude.*` stays byte-identical; `.commodity.seasonality.fitMonthlyFactors` fits a 12-vector seasonal pattern (known-answer tested). On real NG data (`examples/gas_seasonality.q`) the fit recovers the textbook winter premium (Dec/Jan ~+0.16 log-forward) / summer trough (Jun/Jul ~-0.06) and shows schwartz2 alone fits the seasonal gas curve poorly (rmse ~0.12) - motivating the overlay. Part D: `examples/crack_spread_real.q` drives the existing Kirk spread pricer on real correlated crude/products curves (skips gracefully - refined-product data absent). All tests synthetic (no real CSV); real product CSVs are gitignored. No reused-module signature or output changes.

v0.53 (Part A) adds real-data commodity strategy backtests (`lib/commodityStrategies.q`) that test whether the calibration/Kalman outputs are tradeable signals. `.strategy.path.commoditySignals[curveHistory;sigCfg]` builds a signal-augmented path: a roll-adjusted continuous front-RETURN series (hold front, roll N days before expiry, the return is always the held contract's own — no level-stitch jump), plus per-date CAUSAL signals — `convenienceYield` (reuse `convenienceYieldSeries`), Kalman `chi`/`xi`/`chiZ` (Kalman `estimate` runs ONCE on a fixed-`trainEndDate` train split, then the causal forward `filter`), model-free `curveSlopeCarry`, trailing `momentum` — with an `isTrain` flag and a vol-target scale from the train window. Strategies self-register on the existing engine and READ precomputed signals (no model is re-fit in the loop); P&L is the vol-targeted, one-step-lagged `position_{t-1}*frontReturn_t - turnoverCost` under the futures-MTM convention (PV=cash, so deltaPV==stepPnl), all sharing `.strategy.commodityBT.core{Init,Step,Summary}`. EIGHT strategies: Part A — `convenienceYieldCarry` (long backwardation / short contango), `chiReversion` (mean-revert `z=chi/(sigChi/sqrt(2 kappa))` with hysteresis), `realCurveCalendarRoll` (driver around the existing `commodityCalendar`, roll yield by regime); Part B — `timeSeriesMomentum` (trend), `curveRelativeValue` (long cheapest / short richest tenor by per-date schwartz2 residual, trading the precomputed `rvSpreadReturn`), `twoTimescale` (revert chi + trend xi), `storageCashCarry` (long-near/short-far calendar spread when contango exceeds storage cost, trading `nearFarSpreadReturn`), `carryMomentumCombo` (weighted carry+momentum). The shared `.strategy.commodityBT.core{Init,Step}` reads a configurable `returnColumn`/`volScaleColumn` (default front; spread strategies override). `.strategy.commodityBT.runSuite` scores by out-of-sample time-series Sharpe/drawdown/hit-rate (the v0.45 ensemble dashboard is cross-PATH; single-history ranking needs time-series metrics) + a cross-strategy correlation matrix. Tests are synthetic only (roll-adjusted-return hand case; causality — signals unchanged when future is appended; known-answer position signs for all strategies; independent-revaluation accounting at 0 residual; scoring/ranking/correlation arithmetic). The real-data example `examples/commodity_strategy_backtest.q` (train 2020 / test 2021) HONESTLY finds a SPLIT verdict: `curveRelativeValue` had the best out-of-sample edge (Sharpe ~2.2 — the cross-sectional rich/cheap convergence), `timeSeriesMomentum`/`twoTimescale` were modestly positive (trend in the 2021 uptrend), while `convenienceYieldCarry` was slightly negative (the Kalman kappa railed high on the COVID-distorted 2020 train window, making the cy signal noisy) and `chiReversion`/`storageCashCarry` barely traded out-of-sample; the calendar roll's regime decomposition is economically correct (positive roll yield in backwardation, negative in contango). No modification to the parser/calibration/Kalman/engine/ensemble or their signatures.

v0.52 adds a per-date convenience-yield time series and a Schwartz-Smith state-space Kalman-filter MLE that identifies kappa and the vols from the futures panel; demonstrated on real WTI 2020-2021. Part A: `.commodity.curveCal.convenienceYieldSeries[curveHistory;calCfg]` (in `calibrateCurve.q`) calibrates the curve per as-of date across a `.parser.crude.curveHistory` panel with kappa FIXED at `calCfg.kappa` (a single snapshot can't identify it; freeing it per date adds noise) via a degenerate `meanReversionSpeedRange=(kappa;kappa)` — reusing `.commodity.calibrateCurve` unchanged. Non-positive-price dates (the April-2020 negative-settle window) are skipped; returns a series table (asofDate, frontPrice, netConvenienceYield, slope, regime, fitRmse, X0, Y0, muY, status) + skipped dates + regime-transition table. Part B: `.commodity.kalman.*` (new `lib/kalmanSchwartzSmith.q`) implements the two-factor model `ln F(t,T)=e^{-kappa(T-t)}chi_t + xi_t + A(T-t)` as a linear-Gaussian state space (transition `Tm=diag(e^{-kappa dt},1)`, `c=(0;muXi dt)`, process cov `W`; measurement `Z` row `(e^{-kappa tau},1)`, `d=A(tau)`, `V=diag(measSigma^2)`), with `buildMatrices`, a generic `__kalmanStep`/`filterBundles` (Kalman predict/update via `mmu`/`inv`, log-det via `.correlation.__cholesky`, threaded by `scan`), `filter`, `panelFromCurveHistory`, `simulatePanel`, and `estimate` (MLE minimising negative log-likelihood by coordinate descent — a 1-D refining grid per parameter, reusing `.calibration.bestCalibrationResult`; bounded so positives/rho stay in-domain, no transforms). Default frees {kappa, muXi, sigChi, sigXi, rho, measSigma} with risk premia lamChi=lamXi=0; the first date uses dt=0 (identity transition) as a diffuse-prior update; missing tenors per date are handled by the varying observation dimension. The synthetic round-trip (KNOWN params -> panel -> estimate) recovers kappa AND the vols (e.g. ~104-date panel: kappa 1.49 vs 1.5, sigChi 0.26 vs 0.25, sigXi 0.11 vs 0.12) — the identifiability proof a single snapshot could not give. No modification to schwartz2/calibrateCurve/parser/optimiser. Examples (not tests; real gitignored data): `examples/convenience_yield_series.q` (the 2020-2021 backwardation->super-contango->recovery story), `examples/kalman_schwartz_smith.q` (real-panel estimate, kappa~1.95 identified interior, filtered chi/xi capturing the COVID shock).

v0.51 adds an opt-in seasonality overlay and a two-factor Schwartz curve calibration (reusing the existing optimizer), tested by synthetic round-trip and demonstrated on the real WTI Jan-2020 backwardation curve; recovered convenience yield exceeds the rate, as backwardation requires. Part A: `.commodity.seasonality.factor[timeYears;seasonCfg]` (lib/seasonality.q) is a deterministic seasonal log-forward adjustment (smooth `amplitude*cos(2pi*(t-phaseYears))` annual cycle, or an explicit 12-vector `monthlyFactors`); `.strategy.path.fromFuturesCurve`/`fromCorrelatedCurves` take an OPTIONAL `seasonCfg` that shapes each (step,tenor) forward by `exp(factor(deliveryTime))` — when absent the adapters are byte-identical to before (no extra bundle keys, frontPath untouched). Most relevant to gas/power; crude is only mildly seasonal. Part B: `.commodity.calibrateCurve[marketCurve;modelType;calCfg]` (lib/calibrateCurve.q) fits `schwartz2` to a `(tenor,price)` curve (the `.parser.crude.curveAt` shape). Because `log F(tau) = X0*exp(-kappa*tau)+Y0+muY*tau+0.5*v(tau)` is LINEAR in `(X0,Y0,muY)` for fixed kappa, it solves those by exact least squares (q `inv`, convexity extracted from the existing futures function) and searches only kappa on a refining 1-D grid — reusing `.objective.rmse` + `.calibration.bestCalibrationResult`, calling `.commodity.schwartz2.futuresCurve` vectorised, and modifying no existing module. Vols/correlation are FIXED (a single snapshot under-identifies them). Negative/zero prices raise a controlled error (log-price domain; the April-2020 WTI negatives are out of domain); `<3` points or reserved modelTypes (`schwartz`/`mrjump`) also error. Reports `netConvenienceYield = riskFreeRate - frontSlope` (backwardation => convenience yield > carry). Examples (not tests; real gitignored data): `examples/calibrate_crude_curve.q`, `examples/seasonality_curve_demo.q`.

v0.50 adds a CRUDE futures-curve parser (`.parser.crude.*` in the standalone `parser.q`) turning Barchart WTI daily settle CSVs (`Day_CRUDE_YYYYMM00.csv`, columns `Time,Open,High,Low,Latest,Volume`) into `(tenor, price)` forward-curve snapshots for the deferred v0.51 calibration; first real-data path, tested entirely on synthetic in-memory CSV text (real CSVs are gitignored), with the real curve produced via `examples/load_crude_curve.q`. Parsing decisions: date = first 10 chars of the ISO `Time` (the DST-shifting offset is ignored), settle = `Latest`, volume = long; a contract's expiry is the MAX date in its own file and firstDate the MIN (derived from data, not a CME rule — verified: the Jan-2020 file ends 2019-12-19, its real last trade). `curveAt[longTable;asof]` returns alive+quoted contracts sorted by `tenor=(expiry-asof)/365`, dropping any contract with no settle on asof and erroring if none are alive; `curveHistory` gives curve-over-time (column `asofDate` — `asof` is a q built-in). No changes to the equity parser or any pricer/engine/strategy code; no new init load line (extends `parser.q`).

v0.49 (Part A — the spread complex) extends the commodity stack with spread-option pricing, a multi-commodity correlated curve adapter, and two spread-trading strategies, lifting the suite from 322 to 330 tests. A1 — spread-option pricers in `commoditySpread.q`: `.commodity.spread.margrabePrice` (closed-form exchange option, payoff `max(F1−F2,0)`; validated to equal the existing `.commodity.spread.kirkPrice` exactly at strike 0 since Kirk reduces to Margrabe there — diff `0`, and forward parity `call−put = e^{−rT}(F1−F2)` to 7e-15) and `.commodity.spread.spreadOptionMC` (two correlated lognormal forwards, drift `−0.5σ²T`, terminal `max(F1_T−F2_T−K,0)`, reuses `.montecarlo.__generateNormals` + `.montecarlo.priceFromPayoffs` for discounting/SE/CI; agrees with Kirk to <4 SE and with Margrabe at K=0; deterministic for a fixed seed). A2 — `.strategy.path.fromCorrelatedCurves`: Cholesky-correlated GBM fronts (one per commodity, via `.correlation.__cholesky`, identical machinery to `fromCorrelated`) plus additive per-tenor contango, returning one per-name bundle key-compatible with `fromFuturesCurve` output (`frontPath`/`curveSnapshots`/`frontLevels`/…) under a `curves` dict, plus top-level `names`/`correlationMatrix` metadata. A3 — `.strategy.sparkSpread` (power vs `heatRate`·gas) and `.strategy.crackSpread` (product vs `crackRatio`·crude), thin wrappers over a shared `.strategy.spreadOption.*` core: long a Kirk-priced spread option, delta-hedged in BOTH underlying futures using the futures mark-to-market convention (hedge P&L settled to cash each step, futures positions carry zero mark), so `PV = cash + optionMark` and `stepPnl = optionPnl + hedgePnl + financing − txnCost` decomposes deltaPV exactly. Kirk is closed-form (Black-76) so no FDM grid is touched on the path; per-leg deltas are central finite differences. Each strategy self-registers and ships an independent-revaluation accounting test (deltaPV from state components vs result-table stepPnl, max residuals ≤ 4e-15). Parts B (seasonality) and C (calibration) deferred to v0.50. No changes to existing pricers, engine, driver, registry, or shared-hedge-helper signatures; existing 14 strategies + ensemble dashboard byte-identical.

v0.48 is a test-infrastructure milestone — no production code changes. Adds per-test timing to the runner (slowest-20 + per-group subtotals via `.z.p` deltas), a single-group runner (`tests/run_group.q <group>`) that reuses `.test.suites` via a `.test.skipAutoRun` flag, a fast-tier runner (`tests/run_fast_tests.q`), and a top-of-file doc-comment block listing the new commands. Tuned twelve non-canonical accounting/structural tests by coarsening the FDM grid (80×150 → 40×50) and shortening synthetic paths (7-9 → 5 steps); the deltaPV-vs-stepPnl identity holds at machine epsilon regardless of grid resolution, so tolerances were not relaxed. Canonical byte-identical reference tests (`gamma_scalp_synthetic` `totalPnl=0.6521844`, `short_variance_synthetic` `premium=11.97994`, all recon and gate tests) were NOT touched. Full suite wall time: **354 s** (~5.9 min, comfortably under the 10-min ceiling); strategy group 145 s, risk 95 s, stress 34 s, portfolio 28 s, commodity 15 s, all others < 12 s each. Slowest remaining are reference/recon tests that must keep their reference numbers. Trap recorded in `SKILL.md` (extends note 12): a bare `/` line — even one separating documentation comments — opens a multi-line comment block via `\l` and silently disables everything below.

v0.47 completes the v0.46 deferred work in five strategies + two new path adapters, lifting the suite from 9 to 14 strategies. Part B: `.strategy.ironCondor` (4 legs — short OTM put + long further-OTM put + short OTM call + long further-OTM call; held outright with optional delta hedge; analytic max-profit = netCredit, max-loss = widestSpreadWidth − netCredit, breakevens = shortStrike ± netCredit asserted to 1e-12), `.strategy.barrierHedge` (long upAndOut/downAndOut option from product trade fields, delta-hedged via `__hedgeStep`; on knock-out event the option is settled at rebate, hedge closed, position flat for remaining steps; tests use explicit FDM as Crank-Nicolson doesn't support barriers). Part C: `.strategy.jumpPremium` is the structural-model counterpart to modelDisagreement — at entry it prices the option under Merton (series, fast) or Bates (MC, tiny pathCount in tests) AND under BS; jumpPremium = jumpPx0 − bsPx0, gated by absolute/relative threshold; sells when market underprices jump risk (jumpPx0 > bsPx0), forceLong/forceShort overrides; marks on BS along the path for speed. Part D: new path adapter `.strategy.path.fromCorrelated` (Cholesky-correlated multi-asset GBM via `.correlation.__cholesky`, returns per-name path tables + weighted index path), and `.strategy.dispersion` (short index ATM straddle + long weighted constituent ATM straddles, per-underlying hedge book as a TABLE keyed by name, index leg held unhedged for correlation exposure; backs out implied avg correlation from `σ_idx² = w'Σw` and computes realized via pairwise log-return correlation; identity recovery asserted to 1e-10). Part E: new path adapter `.strategy.path.fromFuturesCurve` (`simple` or `mrjump` evolution of the front level + additive contango per tenor; returns frontPath standard-schema + long-format curveSnapshots + jumpCountsAtStep), `.strategy.powerSpikeCapture` (long Black-76 call on the front, gated by jumpCount > 0 OR deviation from mean > threshold·σ; on opening from flat the position-value jump is offset by cash outflow so stepPnl reports only `financing − txnCost`), `.strategy.commodityCalendar` (long near + short far futures on the curve; rolls near leg when elapsedYears ≥ nearTenor, opens next tenor; reuses calendarRoll's leg-table-in-state pattern). One additive multi-asset helper `.strategy.__portfolioValueMulti[cash;legMarkSum;hedgePositions;spots]` for dispersion's accounting test; the single-asset `.strategy.__portfolioValue` is byte-identical. Each new strategy ships an accounting test computing deltaPV from state via the appropriate portfolio-value helper and asserting equality to result-table stepPnl within 1e-8 (max residuals ≤ 7e-15 across all five new strategies). 9 existing strategies + ensemble dashboard byte-identical. No pricing-formula changes; no engine/driver/registry/shared-hedge-helper signature changes.

v0.46 (Part A) adds three more equity-variant strategies on the generic engine: `.strategy.longVol` (mirror of shortVariance — buy ATM straddle when forecastVol > implied vol + entry margin), `.strategy.collarTailHedge` (collar mode: long underlying + long OTM put + short OTM call; tailHedge mode: long OTM puts sized to a premium-budget percent of notional, held outright with no external delta hedge), and `.strategy.putRatioBackspread` (short 1 near-ATM put + long ratioN OTM puts at a lower strike, optional delta hedge). All three self-register, reuse `.strategy.__hedgeStep` for the hedge leg where applicable, and ship accounting tests that compute deltaPV independently via `.strategy.__portfolioValue` and assert equality to result-table stepPnl within 1e-8 (max residuals ≤ 7e-15 across the three new strategies). Existing 9 strategies + ensemble dashboard byte-identical. Parts B–E (defined-risk + barrier; jump premium; correlated multi-asset dispersion; commodity futures-curve adapter + powerSpike + commodityCalendar) intentionally deferred to keep the milestone delivery whole rather than half-done. No pricing-formula changes; no engine/driver/registry/shared-helper changes.

v0.45 completes the strategy suite (`.strategy.riskReversal`, `.strategy.modelDisagreement`, `.strategy.deltaVegaHedge`) and adds a common-random-number multi-path ensemble portfolio runner with a strategy-native performance + correlation dashboard. Added `.strategy.__portfolioValue` helper (PV = cash + legMarkSum + hedgePosition*spot). Three new strategies self-register via the registry. New `.strategy.path.ensemble` for common random numbers across strategies. `.strategy.portfolio.runEnsemble`, `.performanceByStrategy` (mean/std/percentiles/winRate/sharpeLike), `.strategyCorrelation` (N×N matrix), `.dashboard`. Each strategy's accounting test uses `__portfolioValue` to compute deltaPV from state components independently and asserts equality to `stepPnl` from the result table (independent revaluation, not a tautology — max residuals ≤ 2e-14 across the three new strategies and the rewritten calendar test). Existing gamma scalp, short variance, calendar roll outputs unchanged. No pricing-formula changes.

v0.44 adds a calendar-roll strategy (`.strategy.calendarRoll`) on the generic engine — the first with a mutable leg set and roll-event accounting via a portfolio-value identity — validating the registry and driver for path-dependent position lifecycle without engine changes. The leg book is a q TABLE held in state; expiring legs are settled at intrinsic from payoff (never priced via the FDM grid at zero expiry); replacement legs are opened at fresh tenors. Accounting uses `stepPnl == positionPnl + rollPnl + hedgePnl + financingPnl − txnCost` (max identity residual = 0 in the accounting test). gammaReconResidual is computed over non-roll steps only; rolls are reported separately. `.strategy.__hedgeStep` is reused for the hedge leg only; its `positionPnl` / `stepPnl` are discarded because the position mutates. Gamma scalp + short variance outputs unchanged. No pricing-formula changes.

v0.43 adds a short-variance (variance-risk-premium) strategy (`.strategy.shortVariance`) on the generic strategy engine, sharing a single delta-hedge accounting helper (`.strategy.__hedgeStep` + `.strategy.__hedgeInit`) with gamma scalping, and proves registry extensibility on a real second strategy. shortVariance builds a two-leg ATM straddle from the input trade, gates entry on `impliedVol > forecastVol + entryMargin`, collects premium up front, and runs delta-hedged with the same accounting machinery. The driver is untouched. Gamma scalp output is byte-identical post-refactor (totalPnl, intervalRebals, residual all match v0.42 PASS lines). No pricing-formula changes.

v0.42 adds a generic, registry-based strategy/backtest engine (`.strategy`) with data-source-agnostic path adapters (synthetic GBM + Barchart-normalised) and a first delta-hedged gamma-scalping strategy (`.strategy.gammaScalp`) that self-registers at load. The driver `.strategy.run` contains no strategy-specific branching; it folds `stepFn` over path rows via scan and builds the result table column-wise once. Gamma scalping produces per-step P&L attribution (option, hedge, financing, theta, theoretical-gamma) and a `gammaReconResidual` cross-check against the existing greeks engine — within 1% of initial option price on small-step paths. Calls `.engine.priceOption` and `.greeks.calculateGreeks`; no pricing-formula or routing changes.

v0.41 adds in-memory time-series limit history and trend analytics inside `.commodity.modelreport`: `limitSnapshot` (tags a limit table with `runId`/`runDate`/`portfolioName`), `limitSummarySnapshot`, `appendLimitHistory`, `limitHistorySummary` (per-limit observation/OK/warning/breach/error counts plus rates and latest), `limitBreachTrend` (latest vs avg-of-lookback, with worsening/improving/flat direction), `repeatedBreaches`, `limitHistoryDashboard`, and the top-level `runPortfolioRiskWithLimitHistory` wrapper. The wrapper bundles run metadata and existing histories into dicts because q lambdas allow at most 8 parameters. Reporting/aggregation layer only — no persistence and no pricing-formula changes.

v0.40 adds commodity model-risk limit monitoring on top of the portfolio disagreement outputs: `defaultModelRiskLimitConfig`, `checkLimit` (OK/warning/breach/ERROR), `checkPortfolioModelRiskLimits` (seven limits: grossPriceRangeExposure, maxScenarioPnlRange, maxPrimarySensitivityRange, maxVolatilityVegaRange, maxJumpSensitivity, warningAlertCount, errorTradeCount), `limitStatusSummary` (priority ERROR > breach > warning > OK), `limitBreachReport` (sorted, with `breachRank` column — `rank` would shadow q's keyword), and `runPortfolioRiskWithLimits` wrapper. All inputs are float-cast inside `checkLimit` so the resulting table has consistent column types regardless of whether a metric is a long count or a float exposure. Reporting/risk-control layer only — no pricing-formula changes.

v0.39 adds portfolio-level commodity model-disagreement aggregation inside `.commodity.modelreport`: `validatePortfolioPositions`, `runPositionRisk` (tagged per-position tables), `runPortfolioRisk` (uj-merged book of priceRows/greeksRows/scenarioPnlRows/alertRows + positionSummary), `portfolioAlertSummary`, `portfolioDisagreementExposure`, `worstOffenders` (top-N by metric — column is `offenderRank` to avoid shadowing q's `rank`), `portfolioRiskDashboard`, and `runPortfolioDashboard`. Per-position try-catch isolates failures so one bad position never crashes the book. Reporting/aggregation layer only — no pricing-formula changes.

v0.38 adds commodity model-disagreement risk metrics and alerting inside `.commodity.modelreport`: `primarySensitivity`, `priceDisagreement`, `greeksDisagreement`, `scenarioDisagreement`, `disagreementAlerts`, `modelDisagreementReport`, and the top-level `runComparisonRisk` wrapper. Status policy: zero OK rows -> `ERROR; 1..(minimumOkModels-1) -> `warning; otherwise `OK. Reporting-layer extension only — no pricing-formula changes.

v0.37 adds commodity model Greeks/sensitivities (finite-difference) inside `.commodity.modelreport` across all four commodity models, plus a union-joined `greeksAllModels` table, a `greeksSummary` aggregator, and a `runComparisonWithGreeks` convenience wrapper. Common random numbers (shared `mcConfig.randomSeed`) are reused across mrjump base/up/down to reduce FD noise. Reporting/sensitivity layer only — no pricing-formula changes.

v0.36 adds `.commodity.modelreport` for cross-model commodity option comparison and scenario PnL across Black-76, Schwartz one-factor, Schwartz two-factor, and mean-reverting jump models. Reporting layer only — no pricing-formula changes.

v0.35.2 strengthened `.commodity.mrjump` validation with four benchmark tests (lambda-zero call/put vs Schwartz closed-form, stationary-variance limit, jump-intensity monotonicity, jump-mean sensitivity). No pricing logic was changed.
